---
phase: 05-core-infrastructure-and-ipc-protocol
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/mailsync-rs/src/delta/mod.rs
  - app/mailsync-rs/src/delta/item.rs
  - app/mailsync-rs/src/delta/stream.rs
  - app/mailsync-rs/src/delta/flush.rs
  - app/mailsync-rs/src/stdin_loop.rs
  - app/mailsync-rs/src/modes/sync.rs
  - app/mailsync-rs/src/main.rs
  - app/mailsync-rs/tests/ipc_contract.rs
  - app/mailsync-rs/tests/delta_coalesce.rs
  - scripts/start-dev.js
autonomous: true
requirements:
  - IPC-01
  - IPC-02
  - IPC-03
  - IPC-05
  - IPC-06
  - IMPR-08

must_haves:
  truths:
    - "The binary starts in sync mode, prints a startup prompt to stdout, reads account+identity JSON from stdin, and emits a ProcessState delta to stdout"
    - "Delta messages emitted to stdout have exact field names type, modelJSONs, and modelClass (camelCase, not snake_case)"
    - "The binary exits with code 141 when stdin closes (EOF detected)"
    - "stdout is explicitly flushed after every delta batch with no block buffering"
    - "The stdin reader and stdout writer run as independent tokio tasks that do not deadlock under concurrent load"
    - "All 10+ stdin command types parse into a Rust enum; unknown commands log a warning and continue"
    - "Delta coalescing merges two saves of the same model within the flush window into one delta, preserving existing keys"
    - "npm start builds the mailsync-rs binary before launching Electron"
    - "mailsync-process.ts checks for mailsync-rs first, falling back to C++ mailsync"
  artifacts:
    - path: "app/mailsync-rs/src/delta/item.rs"
      provides: "DeltaStreamItem struct with serde renames for modelJSONs and modelClass, plus coalescing logic"
      exports: ["DeltaStreamItem"]
    - path: "app/mailsync-rs/src/delta/stream.rs"
      provides: "DeltaStream wrapper around mpsc sender with emit() and process_state() helpers"
      exports: ["DeltaStream"]
    - path: "app/mailsync-rs/src/delta/flush.rs"
      provides: "delta_flush_task that owns stdout exclusively with explicit flush and 500ms coalescing"
      exports: ["delta_flush_task"]
    - path: "app/mailsync-rs/src/stdin_loop.rs"
      provides: "stdin_loop tokio task with command dispatch enum and EOF detection"
      exports: ["stdin_loop", "StdinCommand"]
    - path: "app/mailsync-rs/src/modes/sync.rs"
      provides: "Sync mode entry point spawning stdin_loop, delta_flush_task, and stub background task"
      exports: ["run"]
    - path: "app/mailsync-rs/tests/ipc_contract.rs"
      provides: "IPC contract tests verifying handshake, delta field names, EOF exit, flush timing, and deadlock resistance"
      min_lines: 120
    - path: "app/mailsync-rs/tests/delta_coalesce.rs"
      provides: "Unit tests for delta coalescing algorithm (key merge, same-type merge, cross-type isolation)"
      min_lines: 60
  key_links:
    - from: "app/mailsync-rs/src/modes/sync.rs"
      to: "app/mailsync-rs/src/delta/flush.rs"
      via: "tokio::spawn(delta_flush_task(delta_rx))"
      pattern: "delta_flush_task"
    - from: "app/mailsync-rs/src/modes/sync.rs"
      to: "app/mailsync-rs/src/stdin_loop.rs"
      via: "tokio::spawn(stdin_loop(...))"
      pattern: "stdin_loop"
    - from: "app/mailsync-rs/src/delta/flush.rs"
      to: "std::io::stdout"
      via: "stdout().lock() + writeln! + flush()"
      pattern: "stdout.*lock.*flush"
    - from: "app/mailsync-rs/src/stdin_loop.rs"
      to: "std::process::exit(141)"
      via: "EOF detection triggers exit code 141"
      pattern: "exit\\(141\\)"
    - from: "scripts/start-dev.js"
      to: "app/mailsync-rs"
      via: "cargo build step before Electron launch"
      pattern: "mailsync-rs"
---

<objective>
Implement the delta emission pipeline with 500ms coalescing, the stdin handshake and command dispatch loop, sync mode skeleton with independent tokio tasks, npm start build integration, and mailsync-process.ts coexistence logic.

Purpose: Completes the IPC protocol — after this plan, the Rust binary can start in sync mode, complete the handshake with Electron, emit ProcessState deltas, accept stdin commands, detect orphan conditions, and coexist alongside the C++ binary during development. Every success criterion from the ROADMAP phase description is covered.

Output: A fully functional sync mode skeleton passing IPC contract tests and delta coalescing tests, plus build pipeline integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-CONTEXT.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-RESEARCH.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-VALIDATION.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-01-SUMMARY.md

@scripts/start-dev.js

<interfaces>
<!-- Types from Plan 01 that this plan depends on -->

From app/mailsync-rs/src/cli.rs (created in Plan 01):
```rust
#[derive(Parser, Debug)]
pub struct Args {
    #[arg(short = 'm', long = "mode", value_enum)]
    pub mode: Mode,
    #[arg(short = 'o', long = "orphan")]
    pub orphan: bool,
    #[arg(short = 'v', long = "verbose")]
    pub verbose: bool,
    #[arg(long = "info")]
    pub info: Option<String>,
    #[arg(short = 'a', long = "account")]
    pub account: Option<String>,
    #[arg(short = 'i', long = "identity")]
    pub identity: Option<String>,
}

#[derive(ValueEnum, Debug, Clone, PartialEq)]
pub enum Mode { Sync, Test, Migrate, Reset, InstallCheck }
```

From app/mailsync-rs/src/account.rs (created in Plan 01):
```rust
pub struct Account { pub id: String, pub email_address: Option<String>, pub provider: Option<String>, ... }
pub struct Identity { pub id: String, ... }
```

From app/mailsync-rs/src/error.rs (created in Plan 01):
```rust
pub enum SyncError { ... }
impl SyncError { pub fn error_key(&self) -> &str; pub fn to_json_error(&self, service: &str) -> Value; }
```

From app/mailsync-rs/src/store/mail_store.rs (created in Plan 01):
```rust
pub struct MailStore { ... }
impl MailStore { pub async fn open(config_dir: &str) -> Result<Self, SyncError>; pub async fn migrate(&self) -> Result<(), SyncError>; }
```

<!-- Key patterns from 05-RESEARCH.md this plan implements -->

Pattern 1 (Handshake): Print "\nWaiting for Account JSON:\n" to stdout + flush -> read account JSON line -> print "\nWaiting for Identity JSON:\n" + flush -> read identity JSON line
Pattern 2 (Delta Wire Format): { "type": "persist"|"unpersist", "modelClass": "...", "modelJSONs": [...] } — exact serde renames required
Pattern 3 (Coalescing): IndexMap<String, Vec<DeltaStreamItem>> buffer, upsert by id within same modelClass+type
Pattern 4 (Flush Task): Dedicated tokio task, std::io::stdout().lock() + writeln! + flush() after each batch, 500ms interval
Pattern 6 (Stdin EOF): next_line() returns Ok(None) -> exit(141), read errors -> 30s grace period then exit(141)
Pattern 8 (Sync Skeleton): 3 tokio tasks — stdin_loop, delta_flush_task, background_sync_stub
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delta emission pipeline (item, stream, flush task) and coalescing tests</name>
  <files>
    app/mailsync-rs/src/delta/mod.rs,
    app/mailsync-rs/src/delta/item.rs,
    app/mailsync-rs/src/delta/stream.rs,
    app/mailsync-rs/src/delta/flush.rs,
    app/mailsync-rs/tests/delta_coalesce.rs
  </files>
  <behavior>
    - Test: DeltaStreamItem serializes with field names "type", "modelClass", "modelJSONs" (not snake_case)
    - Test: DeltaStreamItem::process_state("acct1", false) produces {"type":"persist","modelClass":"ProcessState","modelJSONs":[{"accountId":"acct1","id":"acct1","connectionError":false}]}
    - Test: Coalescing two persist items with same modelClass merges them (single item with both model JSONs)
    - Test: Coalescing same model id twice does key-merge (new values overwrite, existing keys absent in new item are preserved)
    - Test: Coalescing different delta_type (persist vs unpersist) does NOT merge (two separate items)
    - Test: Coalescing different modelClass does NOT merge (separate buffer entries)
    - Test: flush_buffer writes newline-delimited JSON to stdout with explicit flush
  </behavior>
  <action>
    **1. Create `app/mailsync-rs/src/delta/item.rs`:**
    - `DeltaStreamItem` struct with fields:
      - `delta_type: String` — serialized as `"type"` via `#[serde(rename = "type")]`
      - `model_class: String` — serialized as `"modelClass"` via `#[serde(rename = "modelClass")]`
      - `model_jsons: Vec<serde_json::Value>` — serialized as `"modelJSONs"` via `#[serde(rename = "modelJSONs")]`
      - `id_indexes: IndexMap<String, usize>` — `#[serde(skip)]`, internal tracking for upsert
    - `impl DeltaStreamItem`:
      - `pub fn new(delta_type: &str, model_class: &str, model_jsons: Vec<Value>) -> Self` — builds id_indexes from model_jsons
      - `pub fn process_state(account_id: &str, connection_error: bool) -> Self` — creates a ProcessState delta per 05-RESEARCH.md Code Examples section
      - `pub fn concatenate(&mut self, other: &DeltaStreamItem) -> bool` — per 05-RESEARCH.md Pattern 3: if same delta_type and model_class, upsert each JSON from other into self, return true. Otherwise return false.
      - `fn upsert_model_json(&mut self, item: Value)` — per 05-RESEARCH.md Pattern 3: lookup by "id" field, if found: merge keys (new value overwrites existing key, existing keys NOT in new item are preserved). If not found: push and track in id_indexes.
      - `pub fn to_json_string(&self) -> String` — serialize using serde_json::to_string (the serde renames produce correct field names)
    - CRITICAL: Do NOT use `#[serde(rename_all = "camelCase")]` on the struct — it would rename `delta_type` to `deltaType` instead of `type`. Use explicit per-field renames only.

    **2. Create `app/mailsync-rs/src/delta/stream.rs`:**
    - `DeltaStream` struct wrapping `tokio::sync::mpsc::UnboundedSender<DeltaStreamItem>`
    - `impl DeltaStream`:
      - `pub fn new(tx: UnboundedSender<DeltaStreamItem>) -> Self`
      - `pub fn emit(&self, item: DeltaStreamItem)` — sends item via channel. Log warning if channel closed.
      - `pub fn emit_process_state(&self, account_id: &str, connection_error: bool)` — convenience for ProcessState deltas

    **3. Create `app/mailsync-rs/src/delta/flush.rs`:**
    - `pub async fn delta_flush_task(mut rx: mpsc::UnboundedReceiver<DeltaStreamItem>)` per 05-RESEARCH.md Pattern 4:
      - Buffer: `IndexMap<String, Vec<DeltaStreamItem>>` keyed by model_class
      - `tokio::select!` loop:
        - On `rx.recv()`: call `coalesce_into(&mut buffer, item)`
        - On 500ms interval tick: if buffer non-empty, call `flush_buffer(&buffer)` then `buffer.clear()`
        - On channel closed (else branch): flush remaining buffer, return
      - Set `MissedTickBehavior::Skip` on the interval
    - `fn coalesce_into(buffer, item)` — per 05-RESEARCH.md Pattern 3: get or create entry for model_class, try concatenate with last item, push new if different type
    - `fn flush_buffer(buffer)` — per 05-RESEARCH.md Pattern 4:
      - `let stdout = std::io::stdout(); let mut out = stdout.lock();`
      - For each item in buffer values: `writeln!(out, "{}", serde_json::json!({"type": item.delta_type, "modelClass": item.model_class, "modelJSONs": item.model_jsons}))` — use the json! macro for serialization to guarantee exact field names rather than relying on Serialize derive
      - `out.flush().unwrap()` — CRITICAL: explicit flush for pipe mode
      - NOTE: Using `serde_json::json!` macro with string literal keys guarantees the exact field names regardless of struct field names. This is a second layer of defense beyond the serde renames.

    **4. Create `app/mailsync-rs/src/delta/mod.rs`** re-exporting item, stream, flush.

    **5. Create `app/mailsync-rs/tests/delta_coalesce.rs` (Wave 0 test):**
    - Test `test_delta_field_names_match_typescript`: Create a DeltaStreamItem, serialize with to_json_string(), parse back as Value, assert "type", "modelClass", "modelJSONs" keys exist and "model_class", "model_jsons" do NOT exist
    - Test `test_process_state_delta_shape`: Create process_state("acct1", false), verify serialized JSON matches expected shape
    - Test `test_coalesce_same_type_same_class`: Two persist/Message items with different ids -> merged into one item with both JSONs
    - Test `test_coalesce_same_id_key_merge`: Two persist/Message items with same id but different key sets -> merged with new values overwriting, existing keys preserved
    - Test `test_coalesce_different_type_no_merge`: persist/Message + unpersist/Message -> NOT merged (two separate items in buffer)
    - Test `test_coalesce_different_class_no_merge`: persist/Message + persist/Thread -> separate buffer entries
    - Test `test_coalesce_preserves_existing_keys`: Item1 has {id, subject, body}, Item2 has {id, subject} with new value -> merged result has {id, subject (new), body (preserved)}
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test --test delta_coalesce && cargo test --lib -- delta && cargo clippy -- -D warnings</automated>
  </verify>
  <done>
    - DeltaStreamItem serializes with exact C++ field names (type, modelClass, modelJSONs)
    - No snake_case field names leak into serialized JSON
    - ProcessState delta matches the shape expected by OnlineStatusStore.onSyncProcessStateReceived()
    - Coalescing correctly merges same-type/same-class items with key-level merge
    - Coalescing correctly isolates different types and different classes
    - Flush task writes newline-delimited JSON with explicit stdout flush
    - All delta coalesce tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stdin loop, sync mode skeleton, IPC contract tests, and build integration</name>
  <files>
    app/mailsync-rs/src/stdin_loop.rs,
    app/mailsync-rs/src/modes/sync.rs,
    app/mailsync-rs/src/main.rs,
    app/mailsync-rs/tests/ipc_contract.rs,
    scripts/start-dev.js
  </files>
  <behavior>
    - Test: Sync mode prints startup prompt to stdout before reading stdin (handshake trigger)
    - Test: After sending account+identity JSON on stdin, binary emits ProcessState delta with correct field names
    - Test: Closing stdin causes binary to exit with code 141
    - Test: stdout data is received by pipe reader within 500ms of emission (no block buffering)
    - Test: Sending 500KB+ payload on stdin does not deadlock stdout writes
    - Test: All 10+ command types parse without error; unknown commands produce warn log and continue
    - Test: npm start script includes mailsync-rs cargo build step
  </behavior>
  <action>
    **1. Create `app/mailsync-rs/src/stdin_loop.rs`:**
    - `StdinCommand` enum with all C++ command types per 05-CONTEXT.md:
      - QueueTask, CancelTask, WakeWorkers, NeedBodies, SyncCalendar, DetectProvider, QueryCapabilities, SubscribeFolderStatus, TestCrash, TestSegfault
      - Each variant holds relevant data parsed from the JSON command line
    - `fn parse_command(line: &str) -> Option<StdinCommand>` — parse JSON line, extract command type string, match to enum variant. If unknown command: log at warn level, return None.
    - `pub async fn stdin_loop(shutdown_tx: tokio::sync::broadcast::Sender<()>, delta: Arc<DeltaStream>, orphan: bool)`:
      - Create `tokio::io::BufReader::new(tokio::io::stdin()).lines()`
      - Loop on `lines.next_line().await`:
        - `Ok(Some(line))` with non-empty line: parse_command, dispatch (Phase 5: all handlers log at debug level and return — stubs only per 05-CONTEXT.md "No response for stubs")
        - `Ok(Some(""))`: ignore empty lines
        - `Ok(None)`: stdin EOF — if `!orphan` flag: log "stdin EOF", send shutdown via broadcast, sleep 1s for flush, `std::process::exit(141)`. If orphan mode: log but don't exit.
        - `Err(e)`: start 30-second grace period timer per 05-RESEARCH.md Pattern 6. If timer exceeds 30s: exit(141). If next read succeeds: reset timer.
    - IMPORTANT: The stdin BufReader is created once and reused — do NOT create a new BufReader on each iteration.

    **2. Create `app/mailsync-rs/src/modes/sync.rs`:**
    - `pub async fn run(config_dir: &str, account: Account, identity: Option<Identity>, args: &Args) -> Result<(), SyncError>` per 05-RESEARCH.md Pattern 8:
      - Create broadcast shutdown channel: `let (shutdown_tx, _) = broadcast::channel::<()>(1);`
      - Create delta channel: `let (delta_tx, delta_rx) = mpsc::unbounded_channel::<DeltaStreamItem>();`
      - Open MailStore: `let store = Arc::new(MailStore::open(config_dir).await?);`
      - Create DeltaStream: `let delta = Arc::new(DeltaStream::new(delta_tx));`
      - Spawn Task 1 — stdin_loop: `tokio::spawn(stdin_loop(shutdown_tx.clone(), delta.clone(), args.orphan))`
      - Spawn Task 2 — delta_flush_task: `tokio::spawn(delta_flush_task(delta_rx))`
      - Emit startup ProcessState delta: `delta.emit_process_state(&account.id, false);` — this is the delta that tells Electron the account is "online"
      - Spawn Task 3 — background sync stub: `tokio::spawn(background_sync_stub(shutdown_tx.subscribe()))` — this task just waits on the shutdown receiver, doing nothing until Phase 7
      - `tokio::select!` on all three task handles — if any completes, the process is shutting down
    - `async fn background_sync_stub(mut shutdown_rx: broadcast::Receiver<()>)` — just `shutdown_rx.recv().await` to wait for shutdown
    - Per 05-RESEARCH.md: the handshake (reading account+identity JSON from stdin) happens BEFORE entering sync mode run() — main.rs handles the handshake via `read_handshake()`. The sync mode run() receives the already-parsed Account and Identity.

    **3. Update `app/mailsync-rs/src/main.rs`:**
    - Add `mod delta;` and `mod stdin_loop;` module declarations
    - Implement `async fn read_handshake(args: &Args) -> Result<(Account, Option<Identity>), SyncError>`:
      - If `--account` flag provided: parse from flag value. Otherwise: print `"\nWaiting for Account JSON:\n"` to stdout, flush, read one line from tokio::io::stdin()
      - If `--identity` flag provided: parse from flag value. Otherwise: print `"\nWaiting for Identity JSON:\n"` to stdout, flush, read one line. Handle "null" string -> None.
      - CRITICAL: The print to stdout MUST happen BEFORE reading stdin — this is what triggers TypeScript to send the JSON pair. Without this, deadlock.
      - CRITICAL: Use `std::io::stdout().flush()` after each print to ensure the prompt reaches the pipe immediately.
    - Wire sync mode in the dispatch: `Mode::Sync => modes::sync::run(&config_dir, account, identity, &args).await`

    **4. Create `app/mailsync-rs/tests/ipc_contract.rs` (Wave 0 test):**
    - Reuse `binary_path()` and `run_mode()` helpers from mode_tests.rs (or define local copies)
    - Test `test_handshake_and_process_state_delta` (IPC-01, IPC-02):
      - Run `--mode migrate` in tempdir first to create schema
      - Spawn `--mode sync` with CONFIG_DIR_PATH=tempdir, stdin piped, stdout piped
      - In a separate thread: sleep 200ms (let binary print prompt), write account JSON + "\n" + identity JSON + "\n" to stdin, then drop stdin (EOF)
      - Read all stdout, find the ProcessState delta line (contains "ProcessState")
      - Parse as JSON, assert: "type" present, "modelClass" present, "modelJSONs" present, no "model_class" or "model_jsons"
      - Assert: modelClass == "ProcessState", type == "persist"
    - Test `test_stdin_eof_exit_141` (IPC-05):
      - Spawn in sync mode, complete handshake, then close stdin immediately
      - Assert exit code is 141
    - Test `test_stdout_flush_timing` (IPC-06):
      - Spawn in sync mode with piped stdout
      - After handshake, measure time from process start to receiving first ProcessState delta on stdout pipe
      - Assert delta received within 2 seconds (generous timeout — actual should be <500ms)
    - Test `test_no_deadlock_large_stdin` (IMPR-08):
      - Spawn in sync mode, complete handshake
      - In a separate thread: write 500KB+ of valid JSON commands to stdin (e.g., repeated `{"command":"wake-workers"}` lines)
      - Simultaneously read stdout in main thread (don't let pipe buffer fill)
      - Assert process completes without hanging (timeout after 10 seconds = failure)
      - Close stdin -> assert exit 141
    - Test `test_unknown_command_continues` (IPC-03):
      - Spawn in sync mode, complete handshake
      - Write `{"command":"unknown-future-command","data":{}}\n` to stdin
      - Write `{"command":"wake-workers"}\n` to stdin (known command after unknown)
      - Close stdin -> assert exit 141 (process continued past unknown command)
    - All tests use `tempfile::TempDir` for CONFIG_DIR_PATH isolation

    **5. Update `scripts/start-dev.js` to build mailsync-rs:**
    - After the existing mailcore-rs build block (line 58), add a new block:
    - `log(COLORS.yellow, 'Building Rust sync binary (app/mailsync-rs)...');`
    - Use `spawnSync` to run `cargo build` in the workspace root (`app/`) for the `unifymail-sync` package:
      ```
      spawnSync('cargo', ['build', '-p', 'unifymail-sync'], {
        shell: true, stdio: 'inherit', cwd: path.join(__dirname, '..', 'app'), env: buildEnv
      })
      ```
    - On Windows with GNU target: add `--target x86_64-pc-windows-gnu` flag
    - Check exit code, log success or failure (same pattern as mailcore-rs build)
    - This is a debug build (no --release) per 05-CONTEXT.md locked decision

    **6. (Scope note — mailsync-process.ts coexistence):**
    - The `mailsync-process.ts` modification to check for `mailsync-rs` first is a TypeScript change in `app/frontend/mailsync-process.ts`.
    - Add a simple conditional in the binary path resolution: check if `mailsync-rs.exe` (Windows) / `mailsync-rs.bin` (macOS/Linux) exists at the resolved path. If yes, use it. If no, fall back to the C++ `mailsync` binary.
    - The path resolution uses the existing pattern: `path.join(resourcePath, binaryName).replace('app.asar', 'app.asar.unpacked')`
    - For dev mode (--dev flag): look in `app/mailsync-rs/target/debug/` for the binary
    - IMPORTANT: This change must NOT break the existing C++ mailsync flow — the fallback ensures backward compatibility.
    - If this change is too risky for Phase 5 (touching the main sync pipeline), it is acceptable to defer to Phase 6 and document in the SUMMARY that the binary builds but is not yet auto-spawned by Electron. The contract tests prove IPC compatibility independent of the TypeScript integration.
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test --test ipc_contract --test-threads=1 && cargo test --test delta_coalesce && cargo clippy -- -D warnings</automated>
  </verify>
  <done>
    - Sync mode completes the two-line stdin handshake and emits a ProcessState delta with correct field names
    - Binary exits with code 141 when stdin closes
    - stdout is flushed within 500ms (no block buffering in pipe mode)
    - 500KB+ stdin payload does not deadlock stdout writes
    - All 10+ stdin command types parse correctly; unknown commands are logged and ignored
    - Delta coalescing merges same-type/same-class items correctly
    - npm start builds mailsync-rs before Electron launch
    - All IPC contract tests pass
  </done>
</task>

</tasks>

<verification>
- `cd app/mailsync-rs && cargo test --test-threads=1` — all tests pass (mode_tests, ipc_contract, delta_coalesce)
- `cd app/mailsync-rs && cargo clippy -- -D warnings` — no warnings
- `cd app && cargo build` — workspace compiles both crates
- Manual smoke test: `CONFIG_DIR_PATH=/tmp/test cargo run -p unifymail-sync -- --mode sync` with manual stdin input shows ProcessState delta on stdout
</verification>

<success_criteria>
1. The binary starts up, reads the two-line stdin handshake (account JSON then identity JSON), and emits a valid ProcessState delta to Electron without crashing (ROADMAP criterion 1)
2. Delta messages emitted to stdout have exact field names modelJSONs, modelClass, and type (ROADMAP criterion 3)
3. The binary detects stdin EOF and exits with code 141 (ROADMAP criterion 4)
4. stdout is explicitly flushed after every message with no block buffering (ROADMAP criterion 5)
5. The stdin reader and stdout writer run as independent tokio tasks without deadlock (ROADMAP criterion 6)
6. npm start builds the mailsync-rs binary before launching Electron
7. All IPC contract tests and delta coalescing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-infrastructure-and-ipc-protocol/05-02-SUMMARY.md`
</output>
