---
phase: 05-core-infrastructure-and-ipc-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/mailsync-rs/Cargo.toml
  - app/mailsync-rs/Cargo.lock
  - app/mailsync-rs/src/main.rs
  - app/mailsync-rs/src/cli.rs
  - app/mailsync-rs/src/error.rs
  - app/mailsync-rs/src/account.rs
  - app/mailsync-rs/src/store/mod.rs
  - app/mailsync-rs/src/store/mail_store.rs
  - app/mailsync-rs/src/store/migrations.rs
  - app/mailsync-rs/src/modes/mod.rs
  - app/mailsync-rs/src/modes/migrate.rs
  - app/mailsync-rs/src/modes/install_check.rs
  - app/mailsync-rs/src/modes/reset.rs
autonomous: true
requirements:
  - IPC-04
must_haves:
  truths:
    - "Running `cargo build` in app/mailsync-rs/ produces a mailsync binary without errors"
    - "Running the binary with `--mode migrate` creates edgehill.db with all tables, indexes, and FTS5 virtual tables matching the C++ schema"
    - "Running the binary with `--mode install-check` exits with code 0"
    - "Running the binary with `--mode reset` deletes all data for a given account from the database"
    - "The binary uses rustls exclusively -- `cargo tree | grep openssl` returns nothing"
  artifacts:
    - path: "app/mailsync-rs/Cargo.toml"
      provides: "Binary crate definition with all Phase 5 dependencies"
      contains: "[[bin]]"
    - path: "app/mailsync-rs/src/main.rs"
      provides: "tokio main entry point with clap parse and mode dispatch"
      contains: "#[tokio::main]"
    - path: "app/mailsync-rs/src/cli.rs"
      provides: "Clap derive struct with --mode, --verbose, --orphan, --info, --account, --identity flags"
      contains: "ValueEnum"
    - path: "app/mailsync-rs/src/error.rs"
      provides: "SyncError enum with all C++ error key strings"
      contains: "ErrorAuthentication"
    - path: "app/mailsync-rs/src/account.rs"
      provides: "Account and Identity deserialization structs"
      contains: "Account"
    - path: "app/mailsync-rs/src/store/migrations.rs"
      provides: "V1-V9 migration SQL arrays copied verbatim from constants.h"
      contains: "V1_SETUP"
    - path: "app/mailsync-rs/src/store/mail_store.rs"
      provides: "MailStore with open(), migrate(), reset_for_account()"
      contains: "PRAGMA journal_mode = WAL"
  key_links:
    - from: "app/mailsync-rs/src/main.rs"
      to: "app/mailsync-rs/src/modes/migrate.rs"
      via: "Mode::Migrate dispatch"
      pattern: "Mode::Migrate"
    - from: "app/mailsync-rs/src/modes/migrate.rs"
      to: "app/mailsync-rs/src/store/mail_store.rs"
      via: "MailStore::open + migrate()"
      pattern: "MailStore::open"
---

<objective>
Create the Rust mailsync binary crate skeleton with CLI parsing, error types, SQLite schema creation, and the three offline process modes (migrate, install-check, reset). This plan establishes the compilable binary foundation that Plan 02 builds the IPC protocol on top of.

Purpose: Every subsequent phase depends on this binary existing and compiling. The schema SQL is the ground truth for the database -- getting it right here means Phase 6 (model infrastructure) works against a proven schema.

Output: A compilable Rust binary at `app/mailsync-rs/` that handles `--mode migrate`, `--mode install-check`, and `--mode reset` correctly. The `--mode sync` and `--mode test` paths exist as stubs that will be implemented in Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-CONTEXT.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-RESEARCH.md

Source files to reference:
@app/mailsync/MailSync/constants.h (V1-V9 migration SQL -- copy verbatim)
@app/mailcore-rs/Cargo.toml (reference for Cargo workspace patterns)
@app/frontend/mailsync-process.ts (binary path resolution at lines 103-113, error keys at lines 28-83)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Rust binary crate with CLI, error types, and account structs</name>
  <files>
    app/mailsync-rs/Cargo.toml
    app/mailsync-rs/src/main.rs
    app/mailsync-rs/src/cli.rs
    app/mailsync-rs/src/error.rs
    app/mailsync-rs/src/account.rs
    app/mailsync-rs/src/modes/mod.rs
    app/mailsync-rs/src/store/mod.rs
  </files>
  <action>
    Create the `app/mailsync-rs/` directory as a standalone Rust binary crate (NOT a Cargo workspace member -- same standalone pattern as mailcore-rs).

    **Cargo.toml:** Create with these exact dependencies (versions from research):
    ```toml
    [package]
    name = "unifymail-sync"
    version = "2.0.0"
    edition = "2021"

    [[bin]]
    name = "mailsync"
    path = "src/main.rs"

    [dependencies]
    tokio = { version = "1", features = ["rt-multi-thread", "net", "time", "io-util", "macros", "sync", "fs"] }
    tokio-util = { version = "0.7", features = ["codec"] }
    clap = { version = "4", features = ["derive"] }
    serde = { version = "1", features = ["derive"] }
    serde_json = "1"
    rusqlite = { version = "0.38", features = ["bundled"] }
    tokio-rusqlite = "0.7"
    tracing = "0.1"
    tracing-subscriber = { version = "0.3", features = ["env-filter"] }
    tracing-appender = "0.2"
    thiserror = "2"
    indexmap = "2"

    [profile.release]
    lto = true
    strip = "symbols"
    opt-level = 3
    ```

    **cli.rs:** Create clap derive struct matching the C++ option parser:
    ```rust
    use clap::{Parser, ValueEnum};

    #[derive(Parser, Debug)]
    #[command(name = "mailsync")]
    pub struct Args {
        #[arg(short = 'm', long = "mode", value_enum)]
        pub mode: Mode,
        #[arg(short = 'o', long = "orphan")]
        pub orphan: bool,
        #[arg(short = 'v', long = "verbose")]
        pub verbose: bool,
        #[arg(long = "info")]
        pub info: Option<String>,
        #[arg(short = 'a', long = "account")]
        pub account: Option<String>,
        #[arg(short = 'i', long = "identity")]
        pub identity: Option<String>,
    }

    #[derive(ValueEnum, Debug, Clone, PartialEq)]
    pub enum Mode {
        Sync,
        Test,
        Migrate,
        Reset,
        #[value(name = "install-check")]
        InstallCheck,
    }
    ```

    **error.rs:** Create SyncError enum using thiserror with ALL C++ error key strings. The error keys MUST match the exact strings in `LocalizedErrorStrings` in mailsync-process.ts (lines 28-83). Include these variants:
    - ErrorAuthentication, ErrorConnection, ErrorTLSNotAvailable, ErrorCertificate, ErrorParse
    - ErrorGmailIMAPNotEnabled, ErrorGmailExceededBandwidthLimit, ErrorGmailTooManySimultaneousConnections
    - ErrorMobileMeMoved, ErrorYahooUnavailable, ErrorNonExistantFolder, ErrorStartTLSNotAvailable
    - ErrorGmailApplicationSpecificPasswordRequired, ErrorOutlookLoginViaWebBrowser
    - ErrorNeedsConnectToWebmail, ErrorNoValidServerFound, ErrorAuthenticationRequired
    - ErrorInvalidAccount, ErrorIdentityMissingFields
    - ErrorSendMessageNotAllowed, ErrorSendMessageIllegalAttachment
    - ErrorYahooSendMessageSpamSuspected, ErrorYahooSendMessageDailyLimitExceeded
    - ErrorNoSender, ErrorInvalidRelaySMTP, ErrorNoImplementedAuthMethods
    - Plus internal-only variants: Database(String), Protocol(String), Io(String)

    Each error variant must implement a method `error_key(&self) -> &str` returning the exact C++ string (e.g., "ErrorAuthentication"). Also implement `to_error_json(&self) -> serde_json::Value` producing `{"error": "ErrorKey", "error_service": "...", "log": "..."}`.

    **account.rs:** Create Account and Identity structs with serde Deserialize. Use `#[serde(default)]` liberally -- the account JSON has many optional fields. At minimum:
    - Account: id, account_id (renamed from accountId), email_address, provider, imap_host, imap_port, imap_security, smtp_host, smtp_port, smtp_security, imap_username, smtp_username. Use `serde_json::Value` for `settings` to capture all remaining fields.
    - Identity: id, first_name, last_name, email_address, plus `serde_json::Value` for remaining fields.
    - Include `#[serde(rename_all = "camelCase")]` since the JSON from TypeScript uses camelCase.

    **main.rs:** Create `#[tokio::main]` entry point that:
    1. Parses args via `Args::parse()`
    2. Initializes tracing to stderr (NEVER stdout) using `tracing_subscriber::fmt().with_writer(std::io::stderr)` with EnvFilter supporting RUST_LOG. If `--verbose`, set default level to DEBUG; otherwise INFO.
    3. Reads CONFIG_DIR_PATH from env var (required for all modes except install-check).
    4. Dispatches to mode handlers: Migrate and InstallCheck run immediately without stdin. Reset reads account JSON from `--account` flag or stdin. Sync and Test are stubs that print "not yet implemented" to stderr and exit 1 for now (Plan 02 implements them).

    **modes/mod.rs:** Re-export migrate, install_check, reset submodules.
    **store/mod.rs:** Re-export mail_store, migrations submodules.
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo build 2>&1 | tail -5 && cargo tree | grep -c openssl</automated>
  </verify>
  <done>Binary compiles without errors. `cargo tree | grep openssl` returns 0 matches (no OpenSSL). CLI `--help` shows all flags. Tracing writes to stderr only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite schema migrations and offline mode handlers</name>
  <files>
    app/mailsync-rs/src/store/migrations.rs
    app/mailsync-rs/src/store/mail_store.rs
    app/mailsync-rs/src/modes/migrate.rs
    app/mailsync-rs/src/modes/install_check.rs
    app/mailsync-rs/src/modes/reset.rs
  </files>
  <behavior>
    - Test: migrate creates all expected tables (Thread, Message, Folder, Label, Contact, Event, Calendar, Task, File, Account, MessageBody, ModelPluginMetadata, DetatchedPluginMetadata, ThreadReference, ThreadCategory, ThreadCounts, ContactGroup, ContactContactGroup, ContactBook, _State)
    - Test: migrate creates all 3 FTS5 virtual tables (ThreadSearch, EventSearch, ContactSearch)
    - Test: migrate sets PRAGMA user_version to 9
    - Test: migrate is idempotent (running twice does not error)
    - Test: reset_for_account deletes all rows for a given account from all tables
    - Test: WAL mode is active after open
  </behavior>
  <action>
    **migrations.rs:** Copy ALL migration SQL verbatim from `app/mailsync/MailSync/constants.h` (lines 56-225). Create constants V1_SETUP through V9_SETUP as `&[&str]` arrays. Also create ACCOUNT_RESET_QUERIES as `&[&str]`.

    CRITICAL: Copy the SQL exactly as it appears in constants.h. Do NOT simplify, modify column types, or change index names. The C++ schema is the ground truth. Specific notes:
    - V1: 20+ CREATE TABLE + CREATE INDEX statements including 3 FTS5 virtual tables
    - V2: 1 CREATE INDEX
    - V3: ALTER TABLE MessageBody ADD COLUMN fetchedAt + UPDATE
    - V4: DELETE completed tasks + CREATE INDEX
    - V6: DROP and re-CREATE Event table (schema change)
    - V7: ALTER TABLE Event ADD COLUMN icsuid + CREATE INDEX
    - V8: Multiple Contact alterations + 3 new tables (ContactGroup, ContactContactGroup, ContactBook)
    - V9: ALTER TABLE Event ADD COLUMN recurrenceId + CREATE INDEX
    - Note: There is no V5 -- this is intentional (C++ skipped from V4 to V6)

    ACCOUNT_RESET_QUERIES: 19 DELETE statements from constants.h that clean all data for a given account ID, executed as parameterized queries with `?` placeholder for accountId. After all DELETEs, run `DELETE FROM _State WHERE id LIKE ?` with `cursor.%` pattern, then `VACUUM`.

    **mail_store.rs:** Create MailStore struct wrapping a single tokio-rusqlite Connection (single writer is sufficient for Phase 5). Methods:
    - `open(config_dir: &str) -> Result<Self, SyncError>`: Opens `{config_dir}/edgehill.db`, sets PRAGMAs (journal_mode=WAL, page_size=4096, cache_size=10000, synchronous=NORMAL, busy_timeout 10 seconds).
    - `migrate(&self) -> Result<(), SyncError>`: Reads PRAGMA user_version, conditionally applies V1-V9, sets user_version=9. For V3, print "\nRunning Migration" to stdout and flush (Electron shows a migration window when it sees this).
    - `reset_for_account(&self, account_id: &str) -> Result<(), SyncError>`: Executes all ACCOUNT_RESET_QUERIES with the account_id parameter, then vacuums.

    Use `self.conn.call(|conn| { ... }).await` pattern for all database access. Map `tokio_rusqlite::Error` to `SyncError::Database` at the call boundary.

    **migrate.rs:** `pub async fn run(config_dir: &str) -> Result<(), SyncError>` -- opens MailStore, calls migrate(), exits.
    **install_check.rs:** `pub async fn run() -> Result<(), SyncError>` -- prints a success JSON to stdout and exits 0. Output: `{"http_check":{"success":true},"imap_check":{"success":true},"smtp_check":{"success":true},"tidy_check":{"success":true}}`
    **reset.rs:** `pub async fn run(config_dir: &str, account: &Account) -> Result<(), SyncError>` -- opens MailStore, calls reset_for_account with account.id, exits.

    Write integration tests in `app/mailsync-rs/tests/schema_test.rs`:
    - Test that migrate creates all expected tables (query sqlite_master)
    - Test that FTS5 tables exist
    - Test that user_version is 9 after migrate
    - Test that migrate is idempotent
    - Test WAL mode is active
    - Test reset_for_account clears data
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>All schema tests pass. `--mode migrate` creates a database with all 23+ tables, all indexes, and 3 FTS5 virtual tables. PRAGMA user_version is 9. `--mode install-check` exits 0. `--mode reset` clears account data.</done>
</task>

<task type="auto">
  <name>Task 3: Verify offline modes end-to-end via CLI</name>
  <files>
    app/mailsync-rs/src/main.rs
  </files>
  <action>
    Verify the three offline modes work correctly by running the binary directly. This task also ensures the mode dispatch logic in main.rs correctly routes to each handler and that error handling produces the expected JSON output on failure.

    Fix any issues discovered during end-to-end testing. Specific checks:
    1. `--mode migrate` with CONFIG_DIR_PATH set to a temp directory creates edgehill.db
    2. `--mode install-check` exits 0 and prints valid JSON to stdout
    3. `--mode reset` with `--account '{"id":"test-123","accountId":"abc"}'` and CONFIG_DIR_PATH clears data (must run after migrate)
    4. `--mode migrate` run twice on the same database does not error (idempotency)
    5. Running without CONFIG_DIR_PATH prints an error to stderr and exits non-zero (except install-check which does not need it)
    6. `--help` shows all flags correctly

    Also verify `cargo clippy -- -D warnings` passes and `cargo fmt --check` passes. Fix any clippy or formatting issues.
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo build --release 2>&1 | tail -3 && CONFIG_DIR_PATH=$(mktemp -d) cargo run -- --mode migrate 2>/dev/null && CONFIG_DIR_PATH=$(mktemp -d) cargo run -- --mode install-check 2>/dev/null && echo "All offline modes OK"</automated>
  </verify>
  <done>All three offline modes execute successfully. Binary compiles in release mode. clippy and fmt pass. Error cases produce structured JSON output.</done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds in app/mailsync-rs/
2. `cargo test -- --test-threads=1` passes all schema tests
3. `cargo tree | grep openssl` returns nothing (no OpenSSL dependency)
4. `--mode migrate` creates edgehill.db with correct schema
5. `--mode install-check` exits 0
6. `--mode reset` clears account data
7. `cargo clippy -- -D warnings` passes
</verification>

<success_criteria>
The Rust mailsync binary compiles and handles all three offline process modes (migrate, install-check, reset) correctly. The SQLite schema matches the C++ baseline exactly. The binary has no OpenSSL dependencies. The crate is ready for Plan 02 to add the IPC protocol and sync mode.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-infrastructure-and-ipc-protocol/05-01-SUMMARY.md`
</output>
