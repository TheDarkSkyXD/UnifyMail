---
phase: 05-core-infrastructure-and-ipc-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Cargo.toml
  - app/mailcore-rs/Cargo.toml
  - app/mailsync-rs/Cargo.toml
  - app/mailsync-rs/src/main.rs
  - app/mailsync-rs/src/cli.rs
  - app/mailsync-rs/src/error.rs
  - app/mailsync-rs/src/account.rs
  - app/mailsync-rs/src/store/mod.rs
  - app/mailsync-rs/src/store/mail_store.rs
  - app/mailsync-rs/src/store/migrations.rs
  - app/mailsync-rs/src/modes/mod.rs
  - app/mailsync-rs/src/modes/migrate.rs
  - app/mailsync-rs/src/modes/install_check.rs
  - app/mailsync-rs/src/modes/reset.rs
  - app/mailsync-rs/src/modes/test_auth.rs
  - app/mailsync-rs/tests/mode_tests.rs
autonomous: true
requirements:
  - IPC-04

must_haves:
  truths:
    - "Running `mailsync-rs --mode migrate` with CONFIG_DIR_PATH set creates edgehill.db with all 22+ tables, 3 FTS5 virtual tables, and all indexes at schema version 9"
    - "Running `mailsync-rs --mode install-check` exits with code 0 and prints a JSON success response"
    - "Running `mailsync-rs --mode reset` with account JSON deletes all rows for that account from all tables and VACUUMs the database"
    - "The Cargo workspace at app/Cargo.toml successfully builds both mailcore-rs and mailsync-rs members"
  artifacts:
    - path: "app/Cargo.toml"
      provides: "Cargo workspace root with mailcore-rs and mailsync-rs members"
      contains: "[workspace]"
    - path: "app/mailsync-rs/Cargo.toml"
      provides: "Binary crate with all Phase 5 dependencies"
      contains: "[[bin]]"
    - path: "app/mailsync-rs/src/main.rs"
      provides: "Entrypoint with tokio::main and mode dispatch"
      contains: "#[tokio::main]"
    - path: "app/mailsync-rs/src/cli.rs"
      provides: "clap derive Args with --mode, --verbose, --orphan, --info, --account, --identity"
      exports: ["Args", "Mode"]
    - path: "app/mailsync-rs/src/error.rs"
      provides: "SyncError enum with all ~20 C++ error key variants"
      exports: ["SyncError"]
    - path: "app/mailsync-rs/src/account.rs"
      provides: "Account and Identity structs for serde deserialization"
      exports: ["Account", "Identity"]
    - path: "app/mailsync-rs/src/store/mail_store.rs"
      provides: "MailStore with open(), migrate(), reset_for_account()"
      exports: ["MailStore"]
    - path: "app/mailsync-rs/src/store/migrations.rs"
      provides: "V1..V9 migration SQL arrays matching C++ constants.h"
      contains: "V1_SETUP"
    - path: "app/mailsync-rs/tests/mode_tests.rs"
      provides: "Integration tests for migrate, install-check, and reset modes"
      min_lines: 50
  key_links:
    - from: "app/mailsync-rs/src/main.rs"
      to: "app/mailsync-rs/src/cli.rs"
      via: "Args::parse() for mode dispatch"
      pattern: "Args::parse"
    - from: "app/mailsync-rs/src/modes/migrate.rs"
      to: "app/mailsync-rs/src/store/mail_store.rs"
      via: "MailStore::open().migrate()"
      pattern: "MailStore::open"
    - from: "app/mailsync-rs/src/store/mail_store.rs"
      to: "app/mailsync-rs/src/store/migrations.rs"
      via: "V1_SETUP through V9_SETUP SQL arrays"
      pattern: "V[0-9]_SETUP"
    - from: "app/Cargo.toml"
      to: "app/mailcore-rs/Cargo.toml"
      via: "workspace members"
      pattern: "members.*mailcore-rs"
---

<objective>
Create the Rust binary crate scaffold at `app/mailsync-rs/` within a Cargo workspace, implement CLI parsing with clap, define the full error enum matching C++ error keys, implement the SQLite schema migrations (V1-V9) matching constants.h verbatim, and make all three offline modes fully functional: `--mode migrate` (creates/upgrades schema), `--mode install-check` (exits 0), and `--mode reset` (deletes account data).

Purpose: This is the foundation for the entire v2.0 mailsync rewrite. Every subsequent phase builds on this crate structure, error types, and database layer. Getting the schema right here means Phase 6 focuses on model code, not schema changes.

Output: A building Cargo workspace with the mailsync-rs binary crate, passing integration tests for all three offline modes, and a SQLite database matching the C++ schema.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-CONTEXT.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-RESEARCH.md
@app/mailcore-rs/Cargo.toml
@app/mailsync/MailSync/constants.h

<interfaces>
<!-- Key types the executor will create. No existing interfaces to extract — this is a greenfield crate. -->
<!-- The executor MUST reference constants.h for the exact SQL schema strings. -->
<!-- The executor MUST reference mailsync-process.ts lines 163-198 for env vars and spawn behavior. -->

From app/mailcore-rs/Cargo.toml (workspace migration target):
```toml
[package]
name = "mailcore-napi-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "=1.50.0", features = ["rt-multi-thread", "net", "time", "io-util", "macros"] }
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
```

From mailsync-process.ts (env vars passed to binary):
```typescript
const env = {
  ...process.env,
  CONFIG_DIR_PATH: this.configDirPath,
  GMAIL_CLIENT_ID: GMAIL_CLIENT_ID,
  GMAIL_OAUTH_PROXY_URL: GMAIL_OAUTH_PROXY_URL,
  IDENTITY_SERVER: 'unknown',
};
```

From mailsync-process.ts (binary naming):
```typescript
const binaryName = process.platform === 'win32' ? 'mailsync.exe' : 'mailsync.bin';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo workspace, binary crate scaffold, CLI, error types, and account model</name>
  <files>
    app/Cargo.toml,
    app/mailcore-rs/Cargo.toml,
    app/mailsync-rs/Cargo.toml,
    app/mailsync-rs/src/main.rs,
    app/mailsync-rs/src/cli.rs,
    app/mailsync-rs/src/error.rs,
    app/mailsync-rs/src/account.rs,
    app/mailsync-rs/src/store/mod.rs,
    app/mailsync-rs/src/store/mail_store.rs,
    app/mailsync-rs/src/store/migrations.rs,
    app/mailsync-rs/src/modes/mod.rs,
    app/mailsync-rs/src/modes/migrate.rs,
    app/mailsync-rs/src/modes/install_check.rs,
    app/mailsync-rs/src/modes/reset.rs,
    app/mailsync-rs/src/modes/test_auth.rs
  </files>
  <behavior>
    - Test 1: `cargo build` in workspace root succeeds with both mailcore-rs and mailsync-rs members
    - Test 2: `cargo build -p unifymail-sync` produces a binary at target/debug/mailsync-rs(.exe)
    - Test 3: Args parse correctly for all 5 modes: sync, test, migrate, reset, install-check
    - Test 4: SyncError variants map to exact C++ error key strings (ErrorAuthentication, ErrorConnection, etc.)
    - Test 5: SyncError serializes to JSON with exact shape: `{ "error": "ErrorKey", "error_service": "imap"|"smtp", "log": "..." }`
    - Test 6: Account struct deserializes from representative JSON (id, emailAddress, provider, settings)
  </behavior>
  <action>
    **1. Create Cargo workspace root at `app/Cargo.toml`:**
    ```toml
    [workspace]
    members = ["mailcore-rs", "mailsync-rs"]
    resolver = "2"

    [workspace.dependencies]
    tokio = { version = "1", features = ["rt-multi-thread", "net", "time", "io-util", "macros", "sync", "fs"] }
    serde = { version = "1", features = ["derive"] }
    serde_json = "1"
    ```

    **2. Update `app/mailcore-rs/Cargo.toml`** to use workspace dependencies where versions overlap (tokio, serde, serde_json). Keep exact pinned versions for mailcore-rs-specific deps (napi, async-imap, lettre, etc.) since those are proven in v1.0. The key change: add `tokio.workspace = true` etc. for the shared deps OR keep mailcore-rs pinned as-is and only share at workspace level for new crates. Given mailcore-rs uses exact `=` pinning, the SAFEST approach is to NOT migrate mailcore-rs deps to workspace — leave mailcore-rs Cargo.toml unchanged and only define workspace deps for mailsync-rs to consume. This avoids breaking the proven v1.0 addon.

    **3. Create `app/mailsync-rs/Cargo.toml`:**
    - Package name: `unifymail-sync`, version `2.0.0`, edition `2021`
    - `[[bin]]` with `name = "mailsync-rs"` and `path = "src/main.rs"` — the binary name is `mailsync-rs` (not `mailsync`) per coexistence decision
    - Dependencies (reference 05-RESEARCH.md Standard Stack section):
      - tokio (with rt-multi-thread, net, time, io-util, macros, sync, fs)
      - tokio-util (0.7, codec feature)
      - clap (4, derive feature)
      - serde (1, derive feature)
      - serde_json (1)
      - rusqlite (0.38, bundled feature — CRITICAL for FTS5 support)
      - tokio-rusqlite (0.7)
      - tracing (0.1)
      - tracing-subscriber (0.3, env-filter feature)
      - tracing-appender (0.2)
      - thiserror (2)
      - indexmap (2)
    - `[profile.release]` with lto=true, strip="symbols", opt-level=3

    **4. Create `src/cli.rs`** with clap derive:
    - `Args` struct with: `--mode` (Mode enum), `--orphan` (bool), `--verbose` (bool), `--info` (Option<String>), `--account` (Option<String>), `--identity` (Option<String>)
    - `Mode` enum: Sync, Test, Migrate, Reset, InstallCheck (with `#[value(name = "install-check")]`)
    - Reference research Pattern 5 for exact clap derive structure

    **5. Create `src/error.rs`** with SyncError enum using thiserror:
    - All ~20 C++ error key variants: ErrorAuthentication, ErrorConnection, ErrorTLSNotAvailable, ErrorCertificate, ErrorParse, ErrorGmailIMAPNotEnabled, ErrorIdentityMissing, ErrorIMAPConnection, ErrorSMTPConnection, ErrorDatabase, ErrorProtocol, ErrorUnknown, etc.
    - Each variant carries a String message for the `log` field
    - `error_key()` method returning the exact C++ string (e.g., "ErrorAuthentication")
    - `error_service()` method returning "imap", "smtp", or empty
    - `to_json_error()` method producing `{"error": "ErrorKey", "error_service": "...", "log": "..."}` — exact shape from mailsync-process.ts `_spawnAndWait` lines 290-293
    - `is_retryable()` and `is_offline()` classification methods

    **6. Create `src/account.rs`:**
    - `Account` struct with serde Deserialize: id, emailAddress (camelCase rename), provider, settings (as serde_json::Value for now — full typed settings come in Phase 6)
    - `Identity` struct with serde Deserialize: id, plus any fields visible in the C++ source
    - Both structs use `#[serde(rename_all = "camelCase")]` where appropriate

    **7. Create `src/store/migrations.rs`:**
    - Read `app/mailsync/MailSync/constants.h` to extract the EXACT SQL for V1_SETUP_QUERIES through V9_SETUP_QUERIES
    - Each version as `pub const V{N}_SETUP: &[&str] = &[...]`
    - V1: All CREATE TABLE IF NOT EXISTS for _State, File, Event, Label, Folder, Thread, ThreadReference, ThreadCategory, ThreadCounts, Account, Message, ModelPluginMetadata, DetatchedPluginMetadata, MessageBody, Contact, Calendar, Task, ContactGroup, ContactContactGroup, ContactBook — PLUS all CREATE INDEX IF NOT EXISTS — PLUS 3 FTS5 virtual tables (ThreadSearch, EventSearch, ContactSearch)
    - V2: MessageUIDScanIndex
    - V3: ALTER TABLE MessageBody ADD COLUMN fetchedAt + UPDATE
    - V4 through V9: exact SQL from constants.h (V5 is skipped in C++ — goes V4, V6, V7, V8, V9)
    - Also define ACCOUNT_RESET_QUERIES from constants.h — 19 DELETE statements with `?` placeholder for accountId

    **8. Create `src/store/mail_store.rs`:**
    - `MailStore` struct with `writer: tokio_rusqlite::Connection`
    - `open(config_dir: &str)` — opens `{config_dir}/edgehill.db`, runs PRAGMAs: journal_mode=WAL, page_size=4096, cache_size=10000, synchronous=NORMAL, busy_timeout=10s
    - `migrate()` — checks PRAGMA user_version, runs each V{N}_SETUP if version < N, sets user_version=9
    - `reset_for_account(account_id: &str)` — runs all ACCOUNT_RESET_QUERIES with account_id, resets relevant _State rows, then VACUUM
    - `close()` for clean shutdown
    - All writes go through `self.writer.call(|conn| { ... }).await`

    **9. Create mode handlers:**
    - `modes/migrate.rs`: Opens MailStore, calls migrate(), prints "Running Migration" to stdout before V3 (Electron shows migration window), exits 0
    - `modes/install_check.rs`: Prints JSON `{"checks":{"binary":{"success":true}}}` to stdout, exits 0
    - `modes/reset.rs`: Reads account JSON from --account flag (or reads first line from stdin for the account id), opens MailStore, calls reset_for_account(), exits 0
    - `modes/test_auth.rs`: Stub — prints JSON error `{"error":"ErrorNotImplemented","error_service":"","log":"test mode not yet implemented in Rust binary"}` to stderr, exits 1

    **10. Create `src/main.rs`:**
    - `#[tokio::main]` entry point
    - Parse Args via clap
    - Initialize tracing subscriber writing to stderr (NEVER stdout — stdout is reserved for IPC deltas)
    - Read CONFIG_DIR_PATH from env var
    - Mode dispatch: Migrate and InstallCheck run immediately without account JSON; Reset reads account JSON then runs; Test and Sync are stubs for this plan (Sync handled in Plan 02, Test is a minimal stub)
    - On error: print JSON error to stdout using SyncError::to_json_error(), exit 1

    IMPORTANT: Do NOT change mailcore-rs dependency versions or Cargo.toml structure beyond adding the workspace member relationship. The v1.0 addon is proven — do not risk breaking it.
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test --lib && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>
    - Cargo workspace builds both mailcore-rs (unchanged) and mailsync-rs
    - `cargo build -p unifymail-sync` produces a `mailsync-rs` binary
    - All unit tests pass: CLI parsing, error enum serialization, account deserialization
    - clippy and fmt pass with no warnings/errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for offline modes (migrate, install-check, reset)</name>
  <files>
    app/mailsync-rs/tests/mode_tests.rs
  </files>
  <behavior>
    - Test 1: `--mode install-check` exits with code 0 and stdout contains valid JSON
    - Test 2: `--mode migrate` with a temp CONFIG_DIR_PATH creates edgehill.db with correct schema version 9
    - Test 3: After migrate, all expected tables exist (_State, File, Event, Label, Folder, Thread, Message, etc.)
    - Test 4: After migrate, FTS5 virtual tables exist (ThreadSearch, EventSearch, ContactSearch)
    - Test 5: After migrate, key indexes exist (MessageUIDScanIndex, etc.)
    - Test 6: Running migrate twice is idempotent (second run succeeds without error)
    - Test 7: `--mode reset` with account JSON deletes rows for that account and leaves other accounts intact
    - Test 8: After reset, VACUUM succeeds (database is not corrupted)
  </behavior>
  <action>
    Create `app/mailsync-rs/tests/mode_tests.rs` as a Rust integration test that spawns the built binary as a child process (same pattern as v1.0 mailcore-rs integration tests).

    **Test infrastructure:**
    - Use `std::process::Command` to spawn the binary from `target/debug/mailsync-rs` (or `.exe` on Windows)
    - Helper function `run_mode(mode: &str, env: HashMap, stdin: Option<&str>)` that sets up the process, captures stdout/stderr, and returns (exit_code, stdout, stderr)
    - Use `tempfile::TempDir` for CONFIG_DIR_PATH isolation between tests (add `tempfile = "3"` to dev-dependencies)

    **install-check test:**
    - Run `mailsync-rs --mode install-check`
    - Assert exit code 0
    - Assert stdout is valid JSON

    **migrate tests:**
    - Run `mailsync-rs --mode migrate` with CONFIG_DIR_PATH pointing to temp dir
    - Assert exit code 0
    - Open `{temp_dir}/edgehill.db` directly with rusqlite
    - Query `PRAGMA user_version` — assert equals 9
    - Query `SELECT name FROM sqlite_master WHERE type='table'` — assert all expected tables exist
    - Query `SELECT name FROM sqlite_master WHERE type='table' AND sql LIKE '%fts5%'` — assert ThreadSearch, EventSearch, ContactSearch exist
    - Query `SELECT name FROM sqlite_master WHERE type='index'` — assert key indexes exist
    - Run migrate AGAIN — assert exit code 0 (idempotent)

    **reset test:**
    - First run migrate to create schema
    - Open DB directly, insert a test Account row and a test Message row for account "test-account-1"
    - Also insert rows for account "test-account-2"
    - Run `mailsync-rs --mode reset --account '{"id":"test-account-1","emailAddress":"test@example.com"}'`
    - Assert exit code 0
    - Open DB, verify test-account-1 rows are gone
    - Verify test-account-2 rows still exist

    Add `tempfile = "3"` and `rusqlite = { version = "0.38", features = ["bundled"] }` to `[dev-dependencies]` in mailsync-rs Cargo.toml.
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test --test mode_tests -- --test-threads=1</automated>
  </verify>
  <done>
    - All 8+ integration tests pass
    - migrate creates a valid SQLite database with schema version 9, all tables, FTS5 tables, and indexes
    - install-check exits 0
    - reset correctly isolates account data deletion
    - Tests run against the actual compiled binary (not in-process), validating the real CLI interface
  </done>
</task>

</tasks>

<verification>
1. `cd app && cargo build` succeeds (workspace builds both members)
2. `cd app/mailsync-rs && cargo test --lib` passes all unit tests
3. `cd app/mailsync-rs && cargo test --test mode_tests -- --test-threads=1` passes all integration tests
4. `cd app/mailsync-rs && cargo clippy -- -D warnings` passes
5. `cd app/mailsync-rs && cargo fmt --check` passes
6. The schema in edgehill.db after migrate matches the C++ constants.h tables exactly
</verification>

<success_criteria>
- Cargo workspace at app/Cargo.toml builds both mailcore-rs (unchanged, v1.0 proven) and mailsync-rs
- The mailsync-rs binary handles --mode migrate, --mode install-check, and --mode reset correctly with proper exit codes
- SQLite schema version is 9 with all 22+ tables, 3 FTS5 virtual tables, and all indexes
- SyncError enum covers all ~20 C++ error keys with exact string mapping
- All tests pass (unit + integration), clippy clean, fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-infrastructure-and-ipc-protocol/05-01-SUMMARY.md`
</output>
