---
phase: 05-core-infrastructure-and-ipc-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Cargo.toml
  - app/mailcore-rs/Cargo.toml
  - app/mailsync-rs/Cargo.toml
  - app/mailsync-rs/src/main.rs
  - app/mailsync-rs/src/cli.rs
  - app/mailsync-rs/src/error.rs
  - app/mailsync-rs/src/account.rs
  - app/mailsync-rs/src/store/mod.rs
  - app/mailsync-rs/src/store/mail_store.rs
  - app/mailsync-rs/src/store/migrations.rs
  - app/mailsync-rs/src/modes/mod.rs
  - app/mailsync-rs/src/modes/migrate.rs
  - app/mailsync-rs/src/modes/install_check.rs
  - app/mailsync-rs/src/modes/reset.rs
  - app/mailsync-rs/src/modes/test_auth.rs
  - app/mailsync-rs/tests/mode_tests.rs
autonomous: true
requirements:
  - IPC-04

must_haves:
  truths:
    - "Running `cargo build` in app/mailsync-rs produces a mailsync-rs binary"
    - "Running with --mode migrate creates edgehill.db at CONFIG_DIR_PATH with schema version 9 and all 22+ tables including 3 FTS5 virtual tables"
    - "Running --mode migrate twice is idempotent (no errors, same schema)"
    - "Running with --mode install-check exits with code 0"
    - "Running with --mode reset deletes all data for the specified account only, leaving other account data intact"
    - "Running with --mode test returns a not-implemented error and exits with code 1"
    - "Cargo workspace at app/Cargo.toml includes both mailcore-rs and mailsync-rs as members with shared dependency versions"
  artifacts:
    - path: "app/Cargo.toml"
      provides: "Cargo workspace root with mailcore-rs and mailsync-rs members"
      contains: "[workspace]"
    - path: "app/mailsync-rs/Cargo.toml"
      provides: "Binary crate with all Phase 5 dependencies"
      contains: "[[bin]]"
    - path: "app/mailsync-rs/src/main.rs"
      provides: "Entry point with tokio runtime and mode dispatch"
      contains: "#[tokio::main]"
    - path: "app/mailsync-rs/src/cli.rs"
      provides: "clap derive Args struct with --mode, --verbose, --orphan, --info, --account, --identity flags"
      exports: ["Args", "Mode"]
    - path: "app/mailsync-rs/src/error.rs"
      provides: "SyncError enum with all ~20 C++ error key variants"
      exports: ["SyncError"]
    - path: "app/mailsync-rs/src/account.rs"
      provides: "Account and Identity deserialization structs"
      exports: ["Account", "Identity"]
    - path: "app/mailsync-rs/src/store/mail_store.rs"
      provides: "MailStore with open, migrate, and reset_for_account methods"
      exports: ["MailStore"]
    - path: "app/mailsync-rs/src/store/migrations.rs"
      provides: "V1-V9 migration SQL arrays copied verbatim from C++ constants.h"
      contains: "V1_SETUP"
    - path: "app/mailsync-rs/tests/mode_tests.rs"
      provides: "Integration tests for all offline modes"
      min_lines: 80
  key_links:
    - from: "app/mailsync-rs/src/main.rs"
      to: "app/mailsync-rs/src/cli.rs"
      via: "clap Args::parse()"
      pattern: "Args::parse"
    - from: "app/mailsync-rs/src/main.rs"
      to: "app/mailsync-rs/src/modes/"
      via: "Mode enum dispatch in match"
      pattern: "Mode::Migrate|Mode::InstallCheck|Mode::Reset"
    - from: "app/mailsync-rs/src/modes/migrate.rs"
      to: "app/mailsync-rs/src/store/mail_store.rs"
      via: "MailStore::open + migrate"
      pattern: "MailStore::open.*migrate"
    - from: "app/Cargo.toml"
      to: "app/mailcore-rs/Cargo.toml"
      via: "workspace members list"
      pattern: "members.*mailcore-rs"
---

<objective>
Create the Rust mailsync-rs binary crate scaffold with Cargo workspace, CLI parsing, error types, SQLite schema migrations, and all offline process modes (migrate, install-check, reset, test).

Purpose: Establishes the binary foundation that every subsequent Phase 5-10 task builds on. The offline modes (migrate, install-check, reset) are fully functional — only sync mode (Plan 02) remains as a stub.

Output: A compilable `app/mailsync-rs/` binary crate within a `app/Cargo.toml` workspace, passing integration tests for all offline modes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-CONTEXT.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-RESEARCH.md
@.planning/phases/05-core-infrastructure-and-ipc-protocol/05-VALIDATION.md

@app/mailcore-rs/Cargo.toml
@scripts/start-dev.js

<interfaces>
<!-- Key types the executor must create. These are NEW — no existing code to extract from. -->
<!-- The executor should use the 05-RESEARCH.md patterns section as the primary reference. -->

From 05-RESEARCH.md Pattern 5 (Mode Dispatch):
- Args struct (clap derive): --mode (Mode enum), --verbose, --orphan, --info, --account, --identity
- Mode enum: Sync, Test, Migrate, Reset, InstallCheck
- Mode dispatch order: Migrate/InstallCheck before stdin, Reset after account JSON, Test/Sync after identity JSON

From 05-RESEARCH.md Pattern 7 (Schema):
- V1-V9 SQL migration arrays from C++ constants.h
- Database filename: CONFIG_DIR_PATH + "/edgehill.db"
- PRAGMA setup: WAL, page_size=4096, cache_size=10000, synchronous=NORMAL, busy_timeout=10s
- Schema version tracked via PRAGMA user_version
- 22+ tables including 3 FTS5 virtual tables (ThreadSearch, EventSearch, ContactSearch)
- ACCOUNT_RESET_QUERIES: 19 DELETE statements for per-account data removal

From 05-CONTEXT.md (Error Keys):
- Full error enum with ~20 C++ error key strings: ErrorAuthentication, ErrorConnection, ErrorTLSNotAvailable, ErrorCertificate, ErrorParse, ErrorGmailIMAPNotEnabled, etc.
- JSON error shape: { error: "ErrorKey", error_service: "imap"|"smtp", log: "..." }
- Exit codes: 0 success, 1 error, 141 stdin EOF

From 05-CONTEXT.md (Workspace):
- Create app/Cargo.toml workspace with members ["mailcore-rs", "mailsync-rs"]
- Shared workspace dependency versions for tokio, serde, rustls, aws-lc-rs
- mailcore-rs loses its own Cargo.lock, inherits workspace lock
- No shared library crate — independent members with no code coupling
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo workspace, binary crate scaffold, CLI, errors, and account types</name>
  <files>
    app/Cargo.toml,
    app/mailcore-rs/Cargo.toml,
    app/mailsync-rs/Cargo.toml,
    app/mailsync-rs/src/main.rs,
    app/mailsync-rs/src/cli.rs,
    app/mailsync-rs/src/error.rs,
    app/mailsync-rs/src/account.rs,
    app/mailsync-rs/src/modes/mod.rs,
    app/mailsync-rs/src/modes/install_check.rs,
    app/mailsync-rs/src/modes/test_auth.rs,
    app/mailsync-rs/tests/mode_tests.rs
  </files>
  <behavior>
    - Test: `cargo build -p unifymail-sync` compiles the mailsync-rs binary without errors
    - Test: `cargo build -p mailcore-napi-rs` still compiles the mailcore-rs addon (workspace migration did not break it)
    - Test: Running binary with `--mode install-check` exits with code 0
    - Test: Running binary with `--mode test` exits with code 1 and emits JSON error on stdout with "error" field
    - Test: SyncError enum serializes to the exact C++ error key strings (e.g., SyncError::Authentication.error_key() == "ErrorAuthentication")
    - Test: Account struct deserializes from minimal JSON `{"id":"x","emailAddress":"e@x.com","provider":"gmail"}`
  </behavior>
  <action>
    **1. Create Cargo workspace at `app/Cargo.toml`:**
    - `[workspace]` with `members = ["mailcore-rs", "mailsync-rs"]`, `resolver = "2"`
    - Move shared dependency versions to `[workspace.dependencies]`: tokio, serde, serde_json. Do NOT move napi-specific deps or mailsync-specific deps — only deps shared between both crates.
    - The workspace Cargo.lock replaces mailcore-rs's individual Cargo.lock.

    **2. Update `app/mailcore-rs/Cargo.toml`:**
    - Remove `edition = "2021"` if workspace specifies it, OR keep per-crate edition. Key: crate must still compile.
    - For any deps moved to workspace, use `dep.workspace = true` syntax.
    - Delete `app/mailcore-rs/Cargo.lock` if it exists (workspace uses root lock).

    **3. Create `app/mailsync-rs/Cargo.toml`:**
    - Package name: `unifymail-sync`, version `2.0.0`, edition `2021`
    - `[[bin]]` target: `name = "mailsync-rs"`, `path = "src/main.rs"`
    - Dependencies per 05-RESEARCH.md Standard Stack section: tokio (rt-multi-thread, net, time, io-util, macros, sync, fs), tokio-util (codec), clap (derive), serde (derive), serde_json, rusqlite (bundled), tokio-rusqlite, tracing, tracing-subscriber (env-filter), tracing-appender, thiserror, indexmap.
    - Dev-dependencies: `tempfile = "3"` (for mode_tests.rs test isolation)
    - Profile release: lto = true, strip = "symbols", opt-level = 3
    - Use workspace dependencies where applicable (tokio, serde, serde_json).

    **4. Create `app/mailsync-rs/src/cli.rs`:**
    - `Args` struct with clap `#[derive(Parser)]` per 05-RESEARCH.md Pattern 5
    - `Mode` enum with `#[derive(ValueEnum)]`: Sync, Test, Migrate, Reset, InstallCheck (with `#[value(name = "install-check")]`)
    - Flags: `--mode` (required), `--orphan` (bool), `--verbose` (bool), `--info` (Option<String>), `--account` (Option<String>), `--identity` (Option<String>)

    **5. Create `app/mailsync-rs/src/error.rs`:**
    - `SyncError` enum using `#[derive(thiserror::Error)]` with variants:
      - Authentication, Connection, TLSNotAvailable, Certificate, Parse, GmailIMAPNotEnabled, Unexpected, Protocol, Database, Io, Json, NotImplemented
      - Plus remaining C++ error keys: ErrorTimeout, ErrorRetryable, ErrorInvalidCredentials, ErrorExchangeNotEnabled, ErrorIdentityMissing, ErrorYahooSendNotAllowed, etc.
    - `error_key(&self) -> &str` method returning the exact C++ string (e.g., "ErrorAuthentication")
    - `to_json_error(&self, service: &str) -> serde_json::Value` returning `{ error: "ErrorKey", error_service: service, log: "details" }`
    - Implement `From<std::io::Error>`, `From<serde_json::Error>`, `From<rusqlite::Error>` for SyncError

    **6. Create `app/mailsync-rs/src/account.rs`:**
    - `Account` struct with `#[derive(Deserialize)]` — fields: `id: String`, `email_address: Option<String>` (serde rename "emailAddress"), `provider: Option<String>`, plus `#[serde(flatten)] extra: serde_json::Value` to capture all other fields without failing
    - `Identity` struct with `#[derive(Deserialize)]` — fields: `id: String`, plus `#[serde(flatten)] extra: serde_json::Value`

    **7. Create `app/mailsync-rs/src/main.rs`:**
    - `#[tokio::main]` async main
    - Initialize tracing subscriber writing to stderr (NOT stdout): `tracing_subscriber::fmt().with_writer(std::io::stderr).with_env_filter(...)` — if `--verbose` set to DEBUG, else INFO. Respect `RUST_LOG` env var override.
    - Read `CONFIG_DIR_PATH` env var (required for all modes except install-check)
    - Mode dispatch per 05-RESEARCH.md Pattern 5: Migrate and InstallCheck before any stdin, Reset after account JSON, Test/Sync after account+identity JSON
    - For sync mode: placeholder that just calls `todo!("Plan 02 implements sync mode")`
    - On error: write JSON error to stdout via `SyncError::to_json_error()` and exit with code 1

    **8. Create `app/mailsync-rs/src/modes/mod.rs`** re-exporting all mode modules.

    **9. Create `app/mailsync-rs/src/modes/install_check.rs`:**
    - Print the JSON response `{"http_check":{"success":true},"imap_check":{"success":true},"smtp_check":{"success":true},"tidy_check":{"success":true}}` to stdout per 05-RESEARCH.md Open Question 2
    - Exit with code 0

    **10. Create `app/mailsync-rs/src/modes/test_auth.rs`:**
    - Print JSON error `{"error":"ErrorNotImplemented","error_service":"","log":"test mode not yet implemented in Rust binary"}` to stdout per 05-RESEARCH.md Open Question 1
    - Return error so main exits with code 1

    **11. Create `app/mailsync-rs/tests/mode_tests.rs` (Wave 0 test infrastructure):**
    - Helper: `binary_path()` using `env!("CARGO_MANIFEST_DIR")` to locate `target/debug/mailsync-rs.exe` (Windows) / `mailsync-rs` (Unix)
    - Helper: `run_mode(mode, config_dir, stdin_data)` returning (exit_code, stdout, stderr)
    - Test `test_install_check_exits_0`: run --mode install-check, assert exit code 0, assert stdout contains valid JSON
    - Test `test_test_mode_exits_1`: run --mode test with account+identity on stdin, assert exit code 1, assert stdout contains "error" field
    - IMPORTANT: Before running integration tests, ensure the binary is built by adding a `#[cfg(test)]` build step or relying on `cargo test` building the binary target first. Use `cargo_bin` pattern or `assert_cmd` if available, but `Command::new(binary_path())` is fine.

    **12. Verify workspace does not break mailcore-rs:**
    - Run `cd app && cargo build -p mailcore-napi-rs` to confirm it still compiles
    - If workspace dep sharing causes issues with napi-specific deps, keep those as crate-local deps
  </action>
  <verify>
    <automated>cd app && cargo build -p unifymail-sync && cargo build -p mailcore-napi-rs && cd mailsync-rs && cargo test --test mode_tests -- test_install_check test_test_mode && cargo clippy -- -D warnings</automated>
  </verify>
  <done>
    - mailsync-rs binary compiles within the Cargo workspace
    - mailcore-rs still compiles within the workspace (no regression)
    - --mode install-check exits 0 with valid JSON
    - --mode test exits 1 with JSON error
    - SyncError variants map to exact C++ error key strings
    - Account/Identity deserialize from JSON with serde flatten for forward-compatibility
    - Integration tests pass for install-check and test modes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite schema migrations and reset mode with mode integration tests</name>
  <files>
    app/mailsync-rs/src/store/mod.rs,
    app/mailsync-rs/src/store/mail_store.rs,
    app/mailsync-rs/src/store/migrations.rs,
    app/mailsync-rs/src/modes/migrate.rs,
    app/mailsync-rs/src/modes/reset.rs,
    app/mailsync-rs/src/main.rs,
    app/mailsync-rs/tests/mode_tests.rs
  </files>
  <behavior>
    - Test: --mode migrate creates edgehill.db in CONFIG_DIR_PATH with PRAGMA user_version = 9
    - Test: --mode migrate creates all 22+ tables (Thread, Message, Folder, Label, Event, Contact, Calendar, Task, File, Account, etc.)
    - Test: --mode migrate creates 3 FTS5 virtual tables (ThreadSearch, EventSearch, ContactSearch)
    - Test: --mode migrate is idempotent (running twice produces no errors)
    - Test: --mode reset with account JSON deletes rows for that account only (other account data preserved)
    - Test: --mode reset exits 0 on success
    - Test: MailStore::open sets WAL mode, page_size=4096, cache_size=10000, synchronous=NORMAL
  </behavior>
  <action>
    **1. Create `app/mailsync-rs/src/store/migrations.rs`:**
    - Copy ALL migration SQL arrays verbatim from C++ constants.h as referenced in 05-RESEARCH.md Pattern 7
    - `V1_SETUP: &[&str]` — all CREATE TABLE IF NOT EXISTS for: _State, File, Event, Label, Folder, Thread, ThreadReference, ThreadCategory, ThreadCounts, Account, Message, ModelPluginMetadata, DetatchedPluginMetadata, MessageBody, Contact, Calendar, Task, ContactGroup, ContactContactGroup, ContactBook + all indexes + 3 FTS5 virtual tables (ThreadSearch, EventSearch, ContactSearch)
    - `V2_SETUP: &[&str]` — MessageUIDScanIndex
    - `V3_SETUP: &[&str]` — ALTER TABLE MessageBody ADD COLUMN fetchedAt + UPDATE
    - `V4_SETUP`, `V6_SETUP`, `V7_SETUP`, `V8_SETUP`, `V9_SETUP` — subsequent migration SQL
    - Note: There is no V5 in the C++ source (version numbers skip from 4 to 6)
    - `ACCOUNT_RESET_QUERIES: &[&str]` — 19 DELETE statements per 05-RESEARCH.md Code Examples section (DELETE FROM ThreadCounts, ThreadCategory, ThreadSearch, ThreadReference, Thread, File, Event, Label, MessageBody, Message, Task, Folder, ContactSearch, Contact, Calendar, ModelPluginMetadata, DetatchedPluginMetadata, Account)
    - CRITICAL: SQL strings must be EXACT copies from C++ — table names, column names, index names, FTS5 tokenizer config must match the C++ baseline character-for-character.

    **2. Create `app/mailsync-rs/src/store/mail_store.rs`:**
    - `MailStore` struct with `conn: tokio_rusqlite::Connection`
    - `MailStore::open(config_dir: &str) -> Result<Self, SyncError>`:
      - Opens `{config_dir}/edgehill.db`
      - Sets PRAGMAs via `conn.call()`: journal_mode=WAL, page_size=4096, cache_size=10000, synchronous=NORMAL, busy_timeout(Duration::from_secs(10))
      - Returns MailStore
    - `MailStore::migrate(&self) -> Result<(), SyncError>`:
      - Reads `PRAGMA user_version` to get current version
      - Runs each VN_SETUP array only if version < N
      - For V3: emit "Running Migration" to stdout per C++ behavior
      - Sets `PRAGMA user_version = 9` at end if version was < 9
    - `MailStore::reset_for_account(&self, account_id: &str) -> Result<(), SyncError>`:
      - Runs each ACCOUNT_RESET_QUERIES DELETE with account_id as parameter
      - After deletes: `DELETE FROM _State WHERE id LIKE ?` with `%{account_id}%` pattern
      - `VACUUM` to reclaim space
    - `MailStore::close(self)` — consumes self, drops connection

    **3. Create `app/mailsync-rs/src/store/mod.rs`** re-exporting `MailStore`.

    **4. Create `app/mailsync-rs/src/modes/migrate.rs`:**
    - `pub async fn run(config_dir: &str) -> Result<(), SyncError>`
    - Opens MailStore, calls migrate(), closes, returns Ok(())

    **5. Create `app/mailsync-rs/src/modes/reset.rs`:**
    - `pub async fn run(config_dir: &str, account: &Account) -> Result<(), SyncError>`
    - Opens MailStore, calls migrate() first (ensure schema exists), then reset_for_account(&account.id)
    - Note: C++ runs migrate before reset to ensure tables exist for DELETE queries

    **6. Update `app/mailsync-rs/src/main.rs`:**
    - Wire migrate mode: call `modes::migrate::run(&config_dir).await`
    - Wire reset mode: read account JSON (from --account flag or stdin line), call `modes::reset::run(&config_dir, &account).await`
    - For reset mode stdin reading: the C++ reads account JSON as a single line from stdin. In Rust, use `tokio::io::stdin()` + `BufReader::lines()` to read one line, parse as Account.

    **7. Expand `app/mailsync-rs/tests/mode_tests.rs` with schema and reset tests:**
    - Test `test_migrate_creates_schema`: run --mode migrate with tempdir, open the edgehill.db directly with rusqlite, verify `PRAGMA user_version` = 9
    - Test `test_migrate_creates_all_tables`: after migrate, query `SELECT name FROM sqlite_master WHERE type='table'`, verify all 22+ expected table names are present
    - Test `test_migrate_creates_fts5_tables`: verify ThreadSearch, EventSearch, ContactSearch exist (type='table' with FTS5)
    - Test `test_migrate_idempotent`: run migrate twice, verify no errors and same table set
    - Test `test_reset_isolates_account_data`: run migrate, insert test rows for account "acct1" and "acct2" into Thread and Message tables, run --mode reset with account JSON `{"id":"acct1"}`, verify acct1 rows deleted and acct2 rows preserved
    - Test `test_reset_exits_0`: run --mode reset with valid account JSON, assert exit code 0
    - All tests use `tempfile::TempDir` for CONFIG_DIR_PATH isolation
    - Dev-dependency `rusqlite` with `bundled` feature is needed in Cargo.toml for test assertions that open the DB directly
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test --test mode_tests --test-threads=1 && cargo clippy -- -D warnings</automated>
  </verify>
  <done>
    - --mode migrate creates edgehill.db with schema version 9, all 22+ tables, 3 FTS5 virtual tables, all indexes
    - --mode migrate is idempotent (second run produces no errors)
    - --mode reset deletes all data for specified account only (other accounts unaffected)
    - --mode reset exits with code 0
    - WAL mode and PRAGMA settings match C++ baseline
    - All mode integration tests pass with tempdir isolation
    - SQL strings are exact copies from C++ constants.h
  </done>
</task>

</tasks>

<verification>
- `cd app && cargo build` succeeds (workspace compiles)
- `cd app && cargo build -p mailcore-napi-rs` succeeds (no workspace regression)
- `cd app/mailsync-rs && cargo test --test mode_tests --test-threads=1` all pass
- `cd app/mailsync-rs && cargo clippy -- -D warnings` no warnings
- Manual: `cd app/mailsync-rs && cargo run -- --mode install-check` exits 0
- Manual: `CONFIG_DIR_PATH=/tmp/test cargo run -- --mode migrate` creates edgehill.db
</verification>

<success_criteria>
1. The Cargo workspace at `app/Cargo.toml` compiles both mailcore-rs and mailsync-rs
2. All 4 offline modes (migrate, install-check, reset, test) work correctly with proper exit codes
3. Schema migration creates all tables matching the C++ baseline
4. All mode integration tests pass
5. Error types map to exact C++ error key strings
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-infrastructure-and-ipc-protocol/05-01-SUMMARY.md`
</output>
