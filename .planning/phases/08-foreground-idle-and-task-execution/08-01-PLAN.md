---
phase: 08-foreground-idle-and-task-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/mailsync-rs/Cargo.toml
  - app/mailsync-rs/src/tasks/mod.rs
  - app/mailsync-rs/src/tasks/recovery.rs
  - app/mailsync-rs/src/store/task_store.rs
autonomous: true
requirements: [TASK-01, TASK-04, TASK-05]

must_haves:
  truths:
    - "A queue-task stdin command creates a Task record in SQLite with status 'local' and emits a persist delta"
    - "On startup after a crash, tasks stuck in 'remote' state are reset to 'local' and re-queued for execution"
    - "Completed tasks older than the retention period (15 minutes) are deleted automatically on a periodic timer"
  artifacts:
    - path: "app/mailsync-rs/src/tasks/mod.rs"
      provides: "TaskKind enum with serde tag dispatch for all task types, execute_task two-phase orchestration function"
      exports: ["TaskKind", "execute_task"]
    - path: "app/mailsync-rs/src/tasks/recovery.rs"
      provides: "Crash recovery (reset stuck remote tasks) and completed task expiry"
      exports: ["reset_stuck_tasks", "expire_completed_tasks"]
    - path: "app/mailsync-rs/src/store/task_store.rs"
      provides: "Task CRUD helpers: save_task_status, find_local_tasks, save_task_local_changes"
      exports: ["save_task_status", "find_local_tasks"]
  key_links:
    - from: "app/mailsync-rs/src/tasks/mod.rs"
      to: "app/mailsync-rs/src/store/task_store.rs"
      via: "tokio-rusqlite call() for task status updates"
      pattern: "store\\..*task"
    - from: "app/mailsync-rs/src/tasks/recovery.rs"
      to: "app/mailsync-rs/src/store/mail_store.rs"
      via: "tokio-rusqlite call() for SQL UPDATE/DELETE"
      pattern: "UPDATE tasks SET status"
---

<objective>
Create the task processing infrastructure: TaskKind enum for all task types, two-phase execution orchestration (local DB write + delta, then remote placeholder), crash recovery on startup, and completed task expiry timer. Also add lettre dependency with builder feature to Cargo.toml.

Purpose: Establish the task processor framework that Plans 03 and 04 wire into the IDLE loop and fill with remote phase implementations.
Output: tasks/ module with TaskKind enum, execute_task(), recovery functions, and task_store helpers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foreground-idle-and-task-execution/08-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From app/mailsync-rs/src/models/task_model.rs:
```rust
pub struct Task {
    pub id: String,
    pub account_id: String,  // serde rename "aid"
    pub version: i64,        // serde rename "v"
    pub class_name: String,  // serde rename "__cls" — task type name e.g. "SendDraftTask"
    pub status: String,      // "local", "remote", "complete", "cancelled"
    pub should_cancel: Option<bool>,
    pub error: Option<serde_json::Value>,
}
impl MailModel for Task { ... }
```

From app/mailsync-rs/src/store/mail_store.rs:
```rust
pub async fn save<T: MailModel>(&self, model: &mut T) -> Result<(), SyncError>;
pub async fn find<T: MailModel>(&self, id: &str, account_id: &str) -> Result<Option<T>, SyncError>;
```

From app/mailsync-rs/src/delta/stream.rs:
```rust
pub struct DeltaStream { ... }
impl DeltaStream {
    pub fn emit(&self, table_name: &str, delta_type: &str, objects: Vec<serde_json::Value>);
    pub fn emit_process_state(&self, account_id: &str, connection_error: bool);
}
```

From app/mailsync-rs/src/error.rs:
```rust
pub enum SyncError { Authentication, Connection, Database(String), ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskKind enum, two-phase execute_task, and task_store helpers</name>
  <files>
    app/mailsync-rs/Cargo.toml,
    app/mailsync-rs/src/tasks/mod.rs,
    app/mailsync-rs/src/store/task_store.rs,
    app/mailsync-rs/src/store/mod.rs
  </files>
  <behavior>
    - Test: TaskKind deserializes from queue-task JSON with __cls "SendDraftTask" tag
    - Test: TaskKind deserializes ChangeStarredTask, ChangeUnreadTask, ChangeFolderTask, ChangeLabelsTask, DestroyDraftTask, SyncbackMetadataTask, SyncbackEventTask
    - Test: execute_task local phase updates task status from "local" to "remote" in DB
    - Test: execute_task emits persist delta after local phase DB write
    - Test: execute_task updates status to "complete" after successful remote phase
    - Test: save_task_status updates the status column for a given task ID
    - Test: find_local_tasks returns tasks with status "local" ordered by created_at
  </behavior>
  <action>
1. Add lettre to mailsync-rs/Cargo.toml with builder feature:
   ```toml
   lettre = { version = "0.11", default-features = false, features = [
       "builder", "smtp-transport", "tokio1", "tokio1-rustls-tls", "hostname",
   ] }
   ```

2. Create `app/mailsync-rs/src/tasks/mod.rs`:
   - Define `TaskKind` enum with `#[serde(tag = "__cls")]` discriminant matching C++ task type names:
     - `SendDraftTask` — fields: headerMessageId (String), draft data from task JSON
     - `DestroyDraftTask` — fields: messageId, folderId
     - `ChangeStarredTask` — fields: starred (bool), threadIds or messageIds
     - `ChangeUnreadTask` — fields: unread (bool), threadIds or messageIds
     - `ChangeFolderTask` — fields: fromFolderId, toFolderId, threadIds or messageIds
     - `ChangeLabelsTask` — fields: labelsToAdd, labelsToRemove, threadIds or messageIds
     - `SyncbackMetadataTask` — fields: modelId, modelClass, pluginId
     - `SyncbackEventTask` — fields: calendarId, eventId (Phase 9 placeholder, no remote impl)
   - Fields use serde rename to match C++ JSON keys exactly (camelCase).
   - Each variant stores the raw `serde_json::Value` payload alongside typed fields to preserve full task data for DB round-trip.
   - Define `parse_task_kind(task_json: &serde_json::Value) -> Result<TaskKind, SyncError>` that extracts __cls and deserializes.
   - Define `async fn execute_task(task: &mut Task, task_kind: &TaskKind, session: &mut impl ImapOps, store: &MailStore, delta: &DeltaStream) -> Result<(), SyncError>`:
     a. Local phase: set task.status = "remote", save via store, emit persist delta
     b. Remote phase: call `execute_remote(session, task_kind)` (stub returning Ok(()) for now — Plan 04 fills in)
     c. On success: set status = "complete", save, emit delta
     d. On error: set status = "complete" with error field populated, save, emit delta
   - Add `pub mod tasks;` to main.rs module declarations.

3. Create `app/mailsync-rs/src/store/task_store.rs`:
   - `pub async fn save_task_status(store: &MailStore, task_id: &str, status: &str) -> Result<(), SyncError>` — UPDATE tasks SET status WHERE id
   - `pub async fn find_local_tasks(store: &MailStore, account_id: &str) -> Result<Vec<Task>, SyncError>` — SELECT from tasks WHERE status = 'local' AND accountId = ? ORDER BY rowid ASC
   - Add `pub mod task_store;` to store/mod.rs

4. Run `cargo check` in mailsync-rs to verify compilation, then run `cargo test` to verify all tests pass.
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>TaskKind enum deserializes all 8 task types from JSON, execute_task orchestrates local+remote phases with status transitions, task_store helpers compile and pass tests, lettre with builder feature added to Cargo.toml</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crash recovery and completed task expiry</name>
  <files>
    app/mailsync-rs/src/tasks/recovery.rs
  </files>
  <behavior>
    - Test: reset_stuck_tasks changes all "remote" status tasks to "local" and returns count
    - Test: reset_stuck_tasks does not modify "local", "complete", or "cancelled" tasks
    - Test: expire_completed_tasks deletes tasks with status "complete" older than retention period
    - Test: expire_completed_tasks does not delete tasks within the retention window
    - Test: expire_completed_tasks does not delete tasks in "local" or "remote" status
  </behavior>
  <action>
1. Create `app/mailsync-rs/src/tasks/recovery.rs`:
   - `pub async fn reset_stuck_tasks(store: &MailStore) -> Result<usize, SyncError>`:
     Uses tokio-rusqlite call() to execute:
     `UPDATE Task SET data = json_set(data, '$.status', 'local'), status = 'local' WHERE status = 'remote'`
     Returns the number of affected rows. Logs tracing::warn if count > 0.

   - `pub async fn expire_completed_tasks(store: &MailStore, retention_secs: i64) -> Result<usize, SyncError>`:
     Uses tokio-rusqlite call() to execute:
     `DELETE FROM Task WHERE status = 'complete' AND json_extract(data, '$.completed_at') < datetime('now', '-N seconds')`
     where N is retention_secs. Returns deleted count. Logs at debug level.

   - The TASK_RETENTION_SECS constant: 900 (15 minutes, matching C++ TaskProcessor.cpp).

2. Write unit tests using rusqlite in-memory DB with Task table schema:
   - Populate tasks with various statuses, verify reset only touches "remote"
   - Populate tasks with completed_at timestamps, verify expiry only deletes old "complete" tasks

3. Add `pub mod recovery;` to tasks/mod.rs
  </action>
  <verify>
    <automated>cd app/mailsync-rs && cargo test tasks::recovery -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>reset_stuck_tasks resets remote->local on startup, expire_completed_tasks cleans old completed tasks, both have passing unit tests with edge case coverage</done>
</task>

</tasks>

<verification>
- `cargo check` passes in app/mailsync-rs (no compile errors)
- `cargo test` passes all existing + new tests
- TaskKind enum has 8 variants matching C++ task type names
- execute_task transitions: local -> remote -> complete (or complete+error)
- recovery functions handle crash and expiry scenarios
</verification>

<success_criteria>
- TaskKind enum deserializes from C++ queue-task JSON format for all 8 email task types
- execute_task performs local-phase DB write + delta emission before remote phase
- reset_stuck_tasks resets "remote" tasks to "local" on startup
- expire_completed_tasks cleans tasks older than 15 minutes
- lettre with "builder" feature added to Cargo.toml
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/08-foreground-idle-and-task-execution/08-01-SUMMARY.md`
</output>
