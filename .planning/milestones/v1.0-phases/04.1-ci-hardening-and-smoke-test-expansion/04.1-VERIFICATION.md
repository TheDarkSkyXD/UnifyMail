---
phase: 04.1-ci-hardening-and-smoke-test-expansion
verified: 2026-03-03T00:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: null
gaps: []
human_verification: []
---

# Phase 4.1: CI Hardening and Smoke Test Expansion — Verification Report

**Phase Goal:** All CI workflows smoke-test the complete 5-function API surface and build caches work correctly on all platforms
**Verified:** 2026-03-03
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | All 4 CI workflows invoke the same shared smoke test script that checks all 5 exports | VERIFIED | Each of build-linux.yaml (L82-84), build-linux-arm64.yaml (L68-70), build-macos.yaml (L79-81), build-windows.yaml (L77-79) contains exactly `node mailcore-rs/test/smoke.js` with `working-directory: app` |
| 2  | providerForEmail is functionally tested (call with 'test@gmail.com', verify object returned) | VERIFIED | smoke.js L12-18: calls `m.providerForEmail('test@gmail.com')`, checks `result !== null && typeof result === 'object'`, sets `failed = true` and logs `FAIL:` on failure |
| 3  | registerProviders, testIMAPConnection, testSMTPConnection, validateAccount are type-checked (typeof === 'function') | VERIFIED | smoke.js L21-50: all four exports checked with `typeof m.<export> !== 'function'` pattern; each failure sets `failed = true` and exits 1 |
| 4  | macOS CI cache key references package-lock.json (not yarn.lock) | VERIFIED | build-macos.yaml L34: `hashFiles('package-lock.json')` — grep for `yarn.lock` returns no match in that file |
| 5  | Stale auto-generated index.js no longer exists in app/mailcore-rs/ | VERIFIED | `test ! -f app/mailcore-rs/index.js` confirms deletion; `package.json` still has `"main": "loader.js"` |

**Score:** 5/5 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/mailcore-rs/test/smoke.js` | Shared cross-platform smoke test script for all 5 mailcore-napi exports | VERIFIED | Exists, 56 lines (above 30-line minimum), implements all 5 checks: 1 functional + 4 type-checks, exits 1 on any failure, no stubs or placeholders |
| `.github/workflows/build-linux.yaml` | Linux x64 CI with shared smoke test invocation | VERIFIED | Contains `node mailcore-rs/test/smoke.js` at L84 with `working-directory: app` |
| `.github/workflows/build-linux-arm64.yaml` | Linux ARM64 CI with shared smoke test invocation | VERIFIED | Contains `node mailcore-rs/test/smoke.js` at L70 with `working-directory: app` |
| `.github/workflows/build-macos.yaml` | macOS CI with shared smoke test and fixed cache key | VERIFIED | Contains `node mailcore-rs/test/smoke.js` at L81; cache key at L34 uses `hashFiles('package-lock.json')` not `hashFiles('yarn.lock')` |
| `.github/workflows/build-windows.yaml` | Windows CI with shared smoke test invocation | VERIFIED | Contains `node mailcore-rs/test/smoke.js` at L79 with `working-directory: app` |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.github/workflows/build-*.yaml` | `app/mailcore-rs/test/smoke.js` | `node mailcore-rs/test/smoke.js` with `working-directory: app` | WIRED | All 4 workflows contain exactly 1 occurrence of the invocation — grep count confirmed per file |
| `app/mailcore-rs/test/smoke.js` | `app/mailcore-rs/loader.js` | `require('mailcore-napi')` resolves via npm symlink | WIRED | smoke.js L7: `const m = require('mailcore-napi');` — package.json `"main": "loader.js"` unchanged; loader.js exports all 5 functions at L42-48 |
| `.github/workflows/build-macos.yaml` | `package-lock.json` | `hashFiles('package-lock.json')` in cache key | WIRED | build-macos.yaml L34 confirmed; `yarn.lock` not present anywhere in that file |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SCAF-03 (strengthened) | 04.1-01-PLAN.md | GitHub Actions CI builds for all 5 targets — this phase strengthens CI smoke coverage from 1 to 5 exports | SATISFIED | All 4 CI workflows now invoke shared smoke.js that covers all 5 exports; smoke test step wired after napi build, before lint, in every workflow |
| IMAP-01 | 04.1-01-PLAN.md | User can test IMAP connection with TLS — CI now type-checks the testIMAPConnection export is present | SATISFIED | smoke.js L29-33 verifies `typeof m.testIMAPConnection !== 'function'` and fails CI if the export is missing |
| SMTP-01 | 04.1-01-PLAN.md | User can test SMTP connection with TLS — CI now type-checks the testSMTPConnection export is present | SATISFIED | smoke.js L36-40 verifies `typeof m.testSMTPConnection !== 'function'` and fails CI if the export is missing |
| VALD-01 | 04.1-01-PLAN.md | User can validate account concurrently — CI now type-checks the validateAccount export is present | SATISFIED | smoke.js L43-47 verifies `typeof m.validateAccount !== 'function'` and fails CI if the export is missing |

**Note on requirement traceability:** REQUIREMENTS.md traceability table assigns SCAF-03 to Phase 4, IMAP-01 to Phase 2, SMTP-01/VALD-01 to Phase 3. Phase 4.1 is a gap-closure phase that strengthens CI verification of these already-implemented requirements — it does not re-implement them. ROADMAP.md explicitly marks them as "SCAF-03 (strengthened)" etc. This is the correct and expected pattern.

**Orphaned requirements check:** No requirements in REQUIREMENTS.md are assigned to Phase 4.1 by the traceability table. All four IDs in the PLAN are correctly annotated as "strengthened" in the roadmap. No orphaned requirements found.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

Scanned: `app/mailcore-rs/test/smoke.js`, all 4 CI workflow files, `app/mailcore-rs/.gitignore`

No TODO/FIXME/placeholder comments, no empty implementations, no stub handlers found.

---

## Human Verification Required

None. All phase outputs are structural (file existence, file content, string matching) and fully verifiable programmatically.

The SUMMARY.md notes that local verification showed `testIMAPConnection`, `testSMTPConnection`, and `validateAccount` as `undefined` against the pre-built Windows binary in the repo, because that binary predates Phase 2/3 additions. This is a pre-existing environment condition documented by the executor. In CI the binary is built fresh from source before the smoke test step runs, so all 5 exports will be present. This finding does not create a gap — the smoke test logic is correct and will fail CI appropriately if any export is missing.

---

## Gaps Summary

No gaps. All 5 observable truths are verified with direct evidence in the codebase. All 5 required artifacts exist, are substantive (above minimum lines, contain required patterns), and are wired correctly. All 3 key links confirmed. No anti-patterns detected. Both task commits (`4098cab`, `93da7a2`) verified as present in git history.

---

_Verified: 2026-03-03_
_Verifier: Claude (gsd-verifier)_
