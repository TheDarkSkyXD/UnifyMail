---
phase: 04.1-ci-hardening-and-smoke-test-expansion
plan: 01
subsystem: testing
tags: [ci, github-actions, smoke-test, napi-rs, mailcore-napi]

# Dependency graph
requires:
  - phase: 04-cross-platform-packaging-and-cleanup
    provides: "CI workflows with inline smoke tests covering only providerForEmail"
provides:
  - "Shared smoke test at app/mailcore-rs/test/smoke.js covering all 5 mailcore-napi exports"
  - "All 4 CI workflows use shared smoke test script instead of inline node -e code"
  - "macOS CI cache key fixed to use package-lock.json (was yarn.lock, never hit)"
affects: [05-core-infrastructure-and-ipc-protocol, future-ci-additions]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Shared smoke test script invoked via working-directory: app — resolves mailcore-napi via npm symlink"]

key-files:
  created:
    - app/mailcore-rs/test/smoke.js
  modified:
    - .github/workflows/build-linux.yaml
    - .github/workflows/build-linux-arm64.yaml
    - .github/workflows/build-macos.yaml
    - .github/workflows/build-windows.yaml
    - app/mailcore-rs/.gitignore
  deleted:
    - app/mailcore-rs/index.js

key-decisions:
  - "Smoke test uses functional check for providerForEmail (call + object return) and type-checks for the 4 async functions (registering async connections in tests would require live servers)"
  - "test/ directory (singular) used for Node.js smoke tests — not tests/ which is the Cargo integration test directory"
  - "macOS cache key: hashFiles('yarn.lock') -> hashFiles('package-lock.json') — project uses npm, yarn.lock does not exist"

patterns-established:
  - "Smoke test pattern: functional check for synchronous exports, typeof==='function' for async exports"
  - "Shared script invocation: node mailcore-rs/test/smoke.js with working-directory: app"

requirements-completed: [SCAF-03, IMAP-01, SMTP-01, VALD-01]

# Metrics
duration: 3min
completed: 2026-03-04
---

# Phase 04.1 Plan 01: CI Hardening and Smoke Test Expansion Summary

**Shared 5-export smoke test script replaces 4 separate inline one-liners; macOS npm cache key corrected; stale napi-generated index.js removed**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-04T04:09:56Z
- **Completed:** 2026-03-04T04:12:20Z
- **Tasks:** 2
- **Files modified:** 6 (1 created, 4 updated, 1 deleted, 1 gitignore updated)

## Accomplishments

- Created `app/mailcore-rs/test/smoke.js`: functional providerForEmail check plus type-checks for all 5 mailcore-napi exports (was: 4 separate inline one-liners each testing only providerForEmail)
- Updated all 4 CI workflows (build-linux, build-linux-arm64, build-macos, build-windows) to invoke the shared script
- Fixed macOS CI cache key: `hashFiles('yarn.lock')` replaced with `hashFiles('package-lock.json')` — yarn.lock never existed, cache never hit on macOS
- Deleted stale `app/mailcore-rs/index.js`: 579-line napi-generated loader superseded by hand-written `loader.js` (confirmed by package.json `"main": "loader.js"`)
- Updated `.gitignore` comment to reflect that `loader.js` is the hand-written entry point

## Task Commits

Each task was committed atomically:

1. **Task 1: Create shared smoke test script and delete stale index.js** - `4098cab` (feat)
2. **Task 2: Update all 4 CI workflows with shared smoke test and fix macOS cache key** - `93da7a2` (feat)

## Files Created/Modified

- `app/mailcore-rs/test/smoke.js` - Cross-platform smoke test: providerForEmail functional + 4 type-checks; exits 0 on pass, 1 on any failure
- `.github/workflows/build-linux.yaml` - Smoke test step simplified to `node mailcore-rs/test/smoke.js`
- `.github/workflows/build-linux-arm64.yaml` - Smoke test step simplified to `node mailcore-rs/test/smoke.js`
- `.github/workflows/build-macos.yaml` - Smoke test step simplified; cache key yarn.lock -> package-lock.json
- `.github/workflows/build-windows.yaml` - Smoke test step simplified to `node mailcore-rs/test/smoke.js`
- `app/mailcore-rs/.gitignore` - Comment updated: index.js -> loader.js as canonical hand-written loader reference
- `app/mailcore-rs/index.js` - DELETED (stale 579-line napi-generated loader; canonical entry is loader.js)

## Decisions Made

- Functional check only for `providerForEmail` (call with test@gmail.com, verify object returned) — this tests the full provider lookup path. Async functions (testIMAPConnection, testSMTPConnection, validateAccount) use type-check only because calling them requires live servers.
- No `shell:` override needed for Windows workflow — `node` is available in PowerShell (default runner shell); MSYS2 is only needed for the Rust build step.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

Local verification of smoke.js against the repository's pre-built Windows binary showed `testIMAPConnection`, `testSMTPConnection`, and `validateAccount` as `undefined` — the committed `.node` binary predates Phase 2/3 additions. This is a pre-existing environment condition, not caused by this plan's changes. In CI, the binary is built fresh from source before the smoke test step, so all 5 exports will be present and the test will pass.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All 4 CI workflows now validate all 5 mailcore-napi exports on every platform
- macOS CI cache will hit correctly on repeated runs (was silently missing every run)
- Phase 04.1 Plan 02 can proceed (extends coverage to cross-validate JS and Rust provider counts)

---
*Phase: 04.1-ci-hardening-and-smoke-test-expansion*
*Completed: 2026-03-04*
