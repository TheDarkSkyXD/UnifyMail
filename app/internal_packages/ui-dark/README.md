# UnifyMail Dark UI theme

Default dark UI theme for UnifyMail.

This theme is installed by default with UnifyMail and can be activated by going to
the _Themes_ section in the Settings view (`cmd-,`) and selecting it from the
_UI Themes_ drop-down menu.
